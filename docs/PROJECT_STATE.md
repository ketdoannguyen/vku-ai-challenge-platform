# AI Challenge Platform - Current Project State

## 1. Current checkpoint
- Date: 2026-09-21
- Branch: `main` (tích hợp) = `bb22444` (**đã push**, `origin/main` đồng bộ); `release` = `9bd5998` (merge PR #8, 2026-09-21 01:23 UTC) - timer auto-deploy **đã** nhặt: probe production lúc 01:25Z thấy dấu vết ADR-034 (§9)
- Working tree **đang mang đợt ADR-036 chưa commit** - module AI Notebook Review (§2, §4-§10). Chưa commit/push/PR theo yêu cầu, **chưa** vào `release` và **chưa** chạy trên production. Mọi bằng chứng hiện có là unit/component test + một lượt end-to-end trên stack dev (§9). Hai tệp untracked `llm-ai-review.md` và `test-ui-ux.md` vẫn nằm ngoài phạm vi, **không** trộn vào commit
- Commit/working tree status (mốc commit cuối, `main` = `5a529ef`): working tree sạch trước khi mở đợt ADR-036 - đợt **ADR-035** (admin xét duyệt bài nộp) đã commit ở `bb22444`, code + test + hai lượt end-to-end trên stack dev đều đã xanh, và **đã** vào `release` qua PR #8 (cùng ADR-034). Trước đó: đợt sửa lỗi audit UI/UX 2026-09-20 (**ADR-034** + 7 lỗi P0/P2/P3, xem §12 và `.claude/reports/2026-09-20-audit-ui-ux-blackbox.md` mục 0) ở `99eb735`, đã kiểm chứng trên stack dev và cùng vào `release` qua PR #8. Trước đó: đợt **ADR-029→ADR-032** ở `1db367a` (sort/thống kê bảng bài nộp + notebook khung + tên tải một `_`) và `54e1af5` (tìm kiếm/sắp xếp/lọc danh sách cuộc thi), vào `release` qua PR #6. **ADR-033** ở `316dd8e`, vào `release` qua PR #7. **ADR-028** lên production trước đó ở `1cd5945`/`0864b80`
- Overall state: **đã rollout ADR-028 lên production** - backend + frontend test pass; MinIO đã bootstrap trên VM (bucket private, app credential least-privilege); smoke artifact 17/17 + legacy 7/7 + case âm 4/4 qua đúng đường người dùng (Worker → Tunnel → nginx → FastAPI); backup thật + restore drill chạy trọn trên đường production; `vku-deploy.timer` đang bật và đã deploy `0864b80` với `services=none`; `vku-backup.timer` cũng đang bật (hằng ngày ~03:20 UTC) và lượt chạy qua đúng unit systemd đã thành công với đủ 4 tệp. Đợt **ADR-029→ADR-033 đã lên production**. ADR-029→ADR-032 kiểm bằng probe `GET /api/competitions` trên hostname production (payload có `submission_count`, field chỉ ADR-032 sinh ra); ADR-033 kiểm bằng dấu vết deploy (xem §9). **Còn thiếu**: smoke artifact trên production sau release - tên tải đổi `__`→`_` (ADR-031) và object key đổi layout (ADR-033) đều khác thứ đã smoke ở `0864b80`, và chưa ai liệt kê bucket thật trên VM bằng `mc ls` để đối chiếu key. Đợt **ADR-034 + ADR-035 đã vào `release` và đã chạy trên production** (PR #8, merge `9bd5998`, 2026-09-21 01:23 UTC; probe `01:25Z` thấy dấu vết ADR-034 - §9). Bằng chứng hiện có cho hai đợt đều ở mức hạ tầng + stack dev: cả hai đã kiểm trên stack dev thật, riêng ADR-035 thêm hai lượt end-to-end (API 50/50, UI 40/40 - §9), nhưng **chưa** có lượt kiểm nghiệp vụ nào trên production cho ADR-035. Việc còn lại: chạy lại smoke tải artifact trên đường production. Đợt **ADR-036** (kiểm notebook bằng AI) mới ở mức **working tree**: feature tắt mặc định, không đổi hành vi của bất kỳ đường nào đang chạy (submit/score/leaderboard/export không đọc `ai_review`), nên nó **không** chặn hai việc còn lại ở trên

## 2. Implemented capabilities
- Sprint 01-07: local Compose stack, auth Argon2id + server-side session, admin accounts, competition lifecycle, membership/join modes, safe Markdown content/assets, participant portal, scoring + ground truth private, submission policy/quota, my submissions, leaderboard, XLSX export, login rate limit + error envelope + security headers (CSP **enforce** + HSTS từ ADR-034)
- Sprint 08 (ADR-014): đọc công khai cho khách (danh sách, chi tiết, nội dung `public`, assets)
- Vận hành cuộc thi (ADR-015→ADR-019):
  - **Tài nguyên tải về**: `competitions.resources` là link Google Drive (tối đa 10, https, host Drive/Docs, không credentials); không host dataset/binary. Admin quản lý trực tiếp tại tab **Tài nguyên** trong chi tiết cuộc thi (dialog chỉ nhập khi tạo mới); block participant nằm dưới "Mục lục nội dung" trong tab Tổng quan, dùng cùng card shell VKU và format đánh số, ẩn khi rỗng, link ngoài có `rel="noopener noreferrer nofollow"` (ADR-015)
  - **Publish chỉ khi chấm được**: `app/scoring/readiness.py` là một nguồn sự thật, đọc và parse lại ground truth thật; thiếu/sai → 422 với code cụ thể và giữ nguyên `draft`. Admin detail trả `publish_ready`/`publish_blocked_reason` (ADR-017)
  - **Join từ publish đến hết `end_at`**: non-member sau `end_at` → 422 `JOIN_DEADLINE_PASSED`; không kiểm tra `start_at`; membership hiện có luôn idempotent kể cả sau deadline/closed (ADR-017)
  - **Privacy payload**: bỏ `created_by` khỏi representation public; `pos_label` chỉ trả cho admin và thành viên active (ADR-016); datetime naive/aware chuẩn hoá qua một helper dùng chung
  - **Quota trước khi nộp**: `GET /api/competitions/{slug}` trả `quota {per_day,used_today,remaining,resets_at}` cho thành viên active; UI hiện "Còn X/Y lượt" và khoá form khi hết lượt (ADR-019). Từ ADR-034 cổng chặn thật là bộ đếm nguyên tử `quota_day`/`quota_used` trên membership chứ không phải phép đếm theo `completed_at`, nên request song song không vượt được hạn mức
  - **Leaderboard phân trang**: `limit`/`offset`/`has_more` + `me` (hạng toàn cục, tìm trên full list trước khi cắt trang, không có account id) (ADR-019)
  - **Đường thoát**: participant `POST /leave` (soft deactivate, giữ điểm; trigger danger-ghost đỏ và modal xác nhận danger); admin xoá cứng member chỉ khi chưa có bài `completed`, ngược lại 409 `MEMBER_HAS_SUBMISSIONS`; admin `DELETE` competition cascade con-trước-cha-sau và dọn file best-effort (ADR-018)
  - **Countdown sống**: formatter thuần + hook dùng chung, dashboard dùng một page-level clock, nhịp 30 giây trên 1 ngày và 1 giây dưới 1 ngày, resync khi tab visible
- Tổng quan participant: tab Tổng quan là trang thật với thể lệ/quy cách bài nộp/danh sách tài liệu, không tự nhảy sang tài liệu đầu tiên
- Trang tĩnh công khai (ADR-021→ADR-024): `/gioi-thieu` và `/ho-tro` là route tĩnh đọc công khai, **không gọi API** và không đụng backend; navbar có thêm hai mục qua `useNavItems()` (dùng chung navbar desktop và drawer). Cả hai đã redesign sang design system VKU (lưới ô full-width, một cột dưới 1200px, light mode only). Từ ADR-024 **không trang nào còn trích dẫn nguồn thông tin**: `SOURCE_ACCESSED` và `VKU_SOURCES` đã bị xoá, chỉ còn hằng `VKU_DEPARTMENT_URL`; dữ kiện VKU vẫn nằm một chỗ ở `frontend/src/lib/vkuInfo.ts`
- Public entry (ADR-025): Worker `vku-ai-challenge-platform` (Workers Static Assets) phục vụ `dist/` và proxy `/api/*` same-origin, nên cookie phiên host-only + `credentials: "same-origin"` vẫn là hợp đồng duy nhất - không CORS rộng, không JWT. `API_ORIGIN` là runtime variable (không phải biến `VITE_*`, không khai trong `wrangler.jsonc`) nên browser không bao giờ biết địa chỉ backend; `keep_vars` giữ giá trị qua các lần deploy. Proxy **không đọc body**, trả nguyên `Response` của upstream nên `Set-Cookie`, `Content-Disposition` và upload multipart đi nguyên trạng. `frontend/public/_headers` giữ 5 security header cho static (Nginx giữ cho `/api/*`)
- Release tự động (ADR-026): `release` là cổng duy nhất, `main` vẫn là nhánh tích hợp; `release-gate` chạy trên PR vào `release` và trên push vào `main`. Actions deploy Worker (smoke tĩnh quyết định rollback, `/api/health` chỉ là diagnostic); VPS tự kéo bằng `vku-deploy.timer` (`OnUnitInactiveSec=1min`), deployer là bản copy root-owned ở `/usr/local/sbin/vku-auto-deploy` để commit xấu không tự thay cơ chế recovery. Image gắn tag bất biến theo SHA + label `org.opencontainers.image.revision`, `up` luôn `--no-build`, rollback dùng image cũ và kiểm image tồn tại trước khi đụng container
- `closed` không còn terminal (ADR-027): `POST /api/admin/competitions/{id}/reopen` (closed → published, **không** kiểm lại readiness và **không** đụng `end_at`), `DELETE` nhận cả `draft` lẫn `closed` (từ chối `published` bằng 409 `COMPETITION_NOT_DELETABLE`)
- Artifact submission trên MinIO private (ADR-028): mỗi lượt nộp cần **cả** CSV dự đoán **lẫn** notebook `.ipynb`; thiếu/sai một trong hai thì không upload object nào, không tạo document và không tiêu quota. Hai object nằm trong bucket private `submission-artifacts` dưới prefix `competitions/<slug cuộc thi>/accounts/<slug đội>/submissions/submission-NNNN/prediction.csv|notebook.ipynb` (ADR-033; bài nộp trước đó giữ nguyên key theo ObjectId, **không migrate** vì `object_key` lưu nguyên văn trong document), một document Mongo liên kết chúng qua `submission_no` + `artifacts`. Slug đội là field `accounts.slug` sinh tự động từ tên ở lần nộp đầu tiên rồi bất biến; số thứ tự cấp trước khi upload từ counter nguyên tử trên membership nên hai lượt nộp đua nhau không ghi đè key của nhau. **Mọi** đường vào/ra đều qua FastAPI: không presigned URL, không public bucket, không port publish, không route Nginx. Tên file tải về do server đặt (`{slug}_{account}_submission-NNNN_...`, ADR-031) nên không phụ thuộc tên client; record legacy (CSV trên `DATA_DIR`) vẫn đọc/tải được, không migration bắt buộc. Notebook chỉ được validate bằng `json` stdlib, **không bao giờ execute/render**. `/api/health` cố ý **không** phụ thuộc MinIO - MinIO hỏng chỉ làm endpoint artifact trả 503
- Trang quản trị bài nộp toàn cục `/admin/submissions` (ADR-029; đổi từ bảng sang thẻ hai tầng ngày 2026-09-22, thuần trình bày nên không có ADR riêng): lọc theo cuộc thi/status áp **ngay** (không còn nút "Lọc"), tìm đội debounce 300 ms, phân trang server-side, bốn thẻ thống kê (`total`/`competitions`/`teams`/`completed`) tính trên cả tập kết quả khớp bộ lọc. Mỗi bài nộp là **một** item (không còn bảng 12 cột, không còn cuộn ngang) chia hai tầng: tầng nhận diện (Thời gian, Cuộc thi, Đội, **Điểm chính**, **Kết quả**) và tầng xử lý (Tệp đã nộp, Trạng thái, AI sơ bộ, Xét duyệt, Thao tác) — tầng dưới đọc theo trình tự chấm xong chưa → máy nói gì → người chốt gì; **Điểm chính** đứng riêng trong hộp vàng còn cụm **Kết quả** chỉ còn ba metric phụ F1/Precision/Recall, để con số dùng xếp hạng không bị lẫn vào cụm chỉ số. Mỗi trường ở tầng nhận diện có **một ô icon nền màu** (xanh cho thời gian/cuộc thi/kết quả, vàng cho đội/điểm chính) — trang trí thuần, `aria-hidden`, chỉ để mắt phân loại nhanh khi quét danh sách. Chiều cao là ràng buộc chính: mỗi tầng đúng **một dòng** từ 1280px trở lên (thẻ 150px; trước redesign là 235px), dải 768–1279px thì tầng trên xuống hai dòng (thẻ 213–279px) đúng như thiết kế, và giá trị dài cắt bằng ellipsis kèm `title` chứ không xuống dòng. **Ba trục phán quyết là badge có chữ** — hình dạng, màu và chữ cùng nói một kết luận, mỗi badge tự tô theo tông của chính nó; chữ hiện ngay chứ không giấu sau hover vì thiết bị cảm ứng không có hover để trỏ. Điểm nhấn đọc nhanh: cả thẻ là **một nền trắng** với **đúng một vạch 4px ở lề trái** (ba cạnh còn lại là viền mảnh 1px), màu vạch là **mức nặng nhất trong ba trục** theo thang đỏ > vàng > xanh, suy thẳng từ cùng ba hàm tone dựng badge nên thẻ và badge không thể lệch màu; tầng nhận diện thêm một **dải tint rất nhạt** chạy từ vạch vào 35% bề ngang rồi tan hẳn vào nền trắng, nên thẻ vẫn đọc ra là "trắng". Slug cuộc thi không còn hiện trong thẻ, chỉ còn tên dẫn tới trang cuộc thi. Ở 375/414px thẻ cao 584px (mỗi trường một dòng). Lý do xét duyệt, người duyệt, thời điểm và mốc cập nhật AI đi hết vào tooltip nên không thẻ nào cao hơn thẻ nào, và tooltip **không lặp lại** nhãn kết luận vì nhãn đã hiện trên badge. Header có thêm nút **Làm mới** gọi lại đúng trang đang xem, và lần tải đầu hiện **khung xương ba thẻ** thay vì vòng xoay. Bảy trường sắp xếp (thời gian/cuộc thi/đội/F1/Precision/Recall/điểm chính) chuyển từ tiêu đề cột sang thanh sắp xếp + nút đảo chiều, giữ nguyên thứ tự mặc định riêng từng trường. Tab kết quả theo từng cuộc thi dùng chung component nhưng không có trường Cuộc thi, không có lựa chọn sort Cuộc thi và không có thống kê toàn cục; BTC tải được artifact của bất kỳ đội nào
- Notebook khung + trang Hướng dẫn (ADR-030): `GET /api/starter-notebook` công khai phục vụ **một** tệp `backend/app/competitions/starter_notebook.ipynb` đóng trong image (không document Mongo, không bản sao theo cuộc thi, không ghi vào `competitions.resources`); block "Tài nguyên" luôn có mục built-in "Notebook khởi đầu (.ipynb)" và badge đếm bằng `1 + số link ngoài hợp lệ`. Tab **Hướng dẫn** đứng ngay sau Tổng quan, là route công khai không cần đăng nhập, và hiển thị bốn khối: hai artifact bắt buộc + dung lượng, ví dụ CSV theo tên cột thật, yêu cầu notebook v4, và CTA tải notebook khung. Tên cột/bảng mẫu CSV nằm một chỗ ở `frontend/src/lib/submissionRequirements.ts` dùng chung với tab Nộp bài (danh sách lỗi thường gặp chỉ còn ở tab Nộp bài). Notebook khung đi theo tám bước chuẩn, hai bước tiền xử lý và huấn luyện để trống cho thí sinh điền
- Tìm kiếm/sắp xếp/lọc danh sách cuộc thi (ADR-032): trang `/` giữ nguyên ô tìm kiếm theo tên (áp từng ký tự, không nút submit) và ba status pill, thêm nút **Lọc** mở panel qua `createPortal` gồm hai nhóm: sắp xếp (`Tên A–Z` mặc định theo `Intl.Collator("vi")`, `Sắp kết thúc`, `Nhiều lượt nộp nhất`) và tham gia (`Tất cả`/`Đã tham gia`/`Chưa tham gia`, nhóm này **hiện nhưng khóa** với khách). Toàn bộ pipeline chạy **phía client** vì endpoint trả nguyên list và chưa phân trang; panel dùng `fieldset`+radio native nên không tự dựng ARIA menu. Backend chỉ thêm một field derived: `GET /api/competitions` gắn `submission_count` (đếm mọi document submission đã persist, không phụ thuộc `status` **lẫn `review`** - bài bị admin từ chối vẫn là một lượt đã tiêu, ADR-035) bằng **một** aggregation cho cả trang; con số này **chỉ để sắp xếp, không hiển thị trên thẻ**, và detail cố ý không chạy aggregate
- Admin xét duyệt bài nộp (ADR-035): admin đánh dấu một bài **đã chấm** là **Không chấp nhận** kèm lý do bắt buộc (trim, 1–1000 ký tự) qua `PATCH /api/admin/submissions/{id}/review`, và khôi phục lại được (`{"status":"accepted"}`, xoá note). Quyết định nằm ở object tùy chọn `submissions.review` - một **trục độc lập** với `submissions.status` - nên bài bị từ chối vẫn giữ nguyên document, cả hai artifact (participant + admin vẫn tải được), `submission_no`, metrics và lượt quota đã tiêu; scoring vẫn khoá và member có lịch sử vẫn không xoá cứng được. Bài bị từ chối chỉ bị loại khỏi bài "tốt nhất" của participant, leaderboard và XLSX export, tất cả qua **một** predicate dùng chung (`completed` và `review.status != "rejected"`, `eligible_query()` trong `submissions/service.py`) - nên đội còn bài hợp lệ khác thì tự rơi về bài tốt nhất kế tiếp, không thì rời bảng. Participant sở hữu bài thấy badge `Không chấp nhận` + lý do nhưng **không bao giờ** thấy người duyệt/thời điểm; admin thấy đủ trong trường **Xét duyệt** kèm nút `Không chấp nhận`/`Khôi phục`. Chỉ lưu quyết định gần nhất (last write wins), không có event history; không migration, không index mới

- Kiểm tra notebook bằng AI (ADR-036, **chưa commit, chưa lên production**): trục tham khảo **thứ ba**, độc lập với `status` (chấm điểm) và `review` (quyết định của người). Sau khi participant nộp đủ CSV + notebook, chấm điểm vẫn **đồng bộ** và trả ngay; một **worker riêng** (`ai-review-worker`) nhặt job từ queue Mongo và gọi LLM theo giao thức **OpenAI-compatible** (`/chat/completions`) để đối chiếu notebook với thể lệ Markdown của cuộc thi. AI **không bao giờ** ghi `status`, `review`, `metrics`, `primary_score`, counter quota, `submission_no`, metadata artifact hay quyền vào leaderboard - và `eligible_query()` **không** có điều kiện AI. Kết luận của AI chỉ **hạ mức** (`CLEAR`→`FLAGGED`/`INCONCLUSIVE`) chứ không bao giờ nâng lên, không bao giờ tự sinh `ERROR`; rule và bằng chứng được **kiểm lại phía server**, `snippet` do model trả bị vứt và dựng lại từ notebook đã lưu. Cấu hình **theo từng cuộc thi** (`competitions.ai_review_config`, mặc định `enabled:false` nên thiếu/hỏng cấu hình không chặn publish/nộp/chấm): endpoint chỉ cần trỏ tới một hostname/IP **công khai** (ADR-037 đã bỏ allowlist host; mạng nội bộ/metadata vẫn bị chặn và chỉ mở được bằng `AI_REVIEW_ALLOWED_PRIVATE_HOSTS`) và admin phải **tick công khai chấp nhận** rằng nội dung notebook được gửi tới host đó; API key mã hoá **Fernet** bằng `LLM_CONFIG_ENCRYPTION_KEY`, API chỉ trả `api_key_configured: bool`. Bản thể lệ được **snapshot bất biến** lúc nộp (`competition_content_revisions` + `submissions.content_snapshot`), nên kết luận luôn đối chiếu đúng bản thể lệ tại thời điểm nộp. Có cache theo `(cuộc thi, cache_key)` - không bao giờ cache `ERROR`, chạy lại tay luôn bỏ qua cache. Từ ADR-040 model soạn thêm một **gợi ý ngắn (~10 từ)** cho thí sinh (`participant_summary`) - **chỉ admin đọc**, **không bao giờ** đi ra response participant - và gợi ý đó **điền sẵn** ô lý do khi admin bấm "Không chấp nhận"; admin sửa tự do, và chữ được lưu cũng như chữ gửi đi vẫn là bản admin đã duyệt (`review.note`, ADR-035 còn nguyên). Tính năng này **không** thêm storage, endpoint, migration hay mã lỗi nào. Participant chỉ thấy `{state, verdict, summary, updated_at}` khi cuộc thi bật `participant_visible`, **không** thấy mã lỗi kỹ thuật, tên provider/model, finding, bằng chứng hay object key. Notebook vẫn chỉ được validate bằng `json` stdlib, **không** execute/import/render. Không Rule Builder, không slug hard-code
- Giao diện ADR-036: tab **Kiểm tra AI** trong chi tiết cuộc thi admin (`AiReviewSettingsPanel` - bật/tắt, `base_url`, `model`, API key ghi-một-chiều, hai cờ `auto_review`/`participant_visible`, bấm **Kiểm tra kết nối** trước khi lưu, và khối "Nguồn nội dung kiểm tra" cho biết thể lệ nào sẽ được snapshot), trường **AI sơ bộ** trong thẻ bài nộp admin ở cả hai nơi dùng chung component (`none`/`pending`/`clear`/`flagged`/`inconclusive`/`error`) mở `AiReviewDetailModal` xem finding + bằng chứng + lịch sử các `run_id` và bấm chạy lại, và badge/khối AI trong lịch sử participant. Khi bài còn `QUEUED`/`RUNNING`, `usePendingPolling` tự refetch (nhịp 8 s, tối đa 20 lượt) rồi tự dừng

## 3. Not implemented yet (đúng kế hoạch)
- **Pilot với người dùng thật chưa chạy**: hạ tầng đã sẵn sàng (MinIO bootstrapped, backup + drill đã kiểm), nhưng chưa có cuộc thi nào chạy thật với người dùng ngoài
- **Chưa có snapshot ngoài máy**: backup nằm cùng boot disk với dữ liệu, chỉ cứu được lỗi thao tác - cần snapshot GCE hoặc bản sao ngoài (đã ghi ở đầu `scripts/backup_prod.sh`)
- Named Tunnel: chưa có domain nên production tiếp tục dùng Quick Tunnel, hostname đổi mỗi lần `cloudflared` restart và lúc đó `API_ORIGIN` của Worker trỏ vào URL chết
- Public/private leaderboard split - **tạm hoãn có chủ đích** (ADR-020); không giải quyết bằng cách nhân đôi competition
- **AI Notebook Review (ADR-036) chưa chạy ở đâu ngoài stack dev**: chưa commit, chưa vào `release`, chưa có container `ai-review-worker` nào trên production, và **chưa** gọi một provider thật từ production. Production cũng chưa có `LLM_CONFIG_ENCRYPTION_KEY` trong `/srv/vku-ai-challenge/.env` - thiếu biến này thì tính năng tự tắt (đúng chiều ADR-036), nhưng nghĩa là lượt bật đầu tiên vẫn còn là việc chưa làm
- **Cache AI chỉ sống trong một cuộc thi**: `cache_key` gồm `content_hash` của thể lệ, nên hai cuộc thi có cùng thể lệ vẫn gọi model hai lần - cố ý, để kết luận không bao giờ đi lạc sang cuộc thi khác
- **Chưa có thống kê/hàng đợi hiển thị cho admin**: admin chỉ thấy trạng thái AI theo từng bài; không có màn hình "còn bao nhiêu job đang chờ/đang lỗi" (xem bằng `docker compose exec` hoặc Mongo - §9 `docs/DEPLOYMENT.md` §14)

## 4. Repository structure that matters
- `backend/app/submission_artifacts/{storage,validation,naming}.py`: MinIO client qua `run_in_threadpool`, validate notebook bằng `json` stdlib, tên file tải về + `Content-Disposition` (đây là chỗ duy nhất biết `object_key`)
- `backend/app/submissions/artifacts.py`: `artifact_response` dùng chung cho 4 route tải, đọc record mới (MinIO) lẫn record legacy (`DATA_DIR`), chặn path thoát ra ngoài
- `backend/app/submissions/router.py`: POST hai part (`file` + `notebook`), cấp `submission_no` nguyên tử **trước** khi upload (`allocate_submission_no`) rồi mới dựng key, `_cleanup` (xoá object khi upload/insert lỗi), map lỗi storage thành 503
- `backend/app/core/slugs.py`: `is_valid_slug`/`slugify` dùng chung cho competition, nội dung và slug account (bảng dịch `Đ`/`đ` cũng ở đây, `naming.py` import lại)
- `backend/app/accounts/service.py`: `ensure_account_slug` sinh slug đội lazy ở lần nộp đầu tiên (bất biến, va chạm thì `-xxx`), index unique **sparse** trên `slug`
- `backend/app/submissions/admin_router.py`: list theo cuộc thi (allowlist sort **scoped**, không nhận `competition`) và `global_router` `/submissions` (bảng toàn cục, allowlist rộng hơn + `stats`) + hai route tải cho admin + `PATCH /submissions/{id}/review` (ADR-035, gọi `set_submission_review`, lọc `review` trên cả hai GET)
- `backend/app/submissions/service.py`: `SORT_FIELDS`/`SCOPED_SORT_FIELDS`/`sort_spec` (metric sort trên `metrics.<field>`, `$lookup` chỉ cho `team`/`competition`, tie-break `created_at`→`_id`), `submission_stats` (một `$facet` cho bốn số), `eligible_query()` + `set_submission_review()` + `submission_history_item()` (ADR-035)
- `backend/app/competitions/starter_notebook.py` + `starter_notebook.ipynb`: endpoint công khai và asset notebook khung đóng trong image (ADR-030)
- `frontend/src/lib/submissionRequirements.ts`: tên cột hiển thị và bảng mẫu CSV dùng chung cho tab Nộp bài và trang Hướng dẫn; năm lỗi thường gặp chỉ tab Nộp bài dùng
- `frontend/src/pages/CompetitionGuidePage.tsx`: tab Hướng dẫn (route công khai `huong-dan`)
- `backend/app/core/datetimes.py`: `as_utc`, `utc_day_bounds`, `iso_z` dùng chung (naive Mongo được hiểu là UTC)
- `backend/app/scoring/readiness.py`: `check_readiness` / `blocked_reason` cho publish + banner admin + endpoint scoring
- `backend/app/competitions/service.py`: `public_competition` vs `admin_competition`, `normalize_resources`, `delete_competition_cascade`, `remove_competition_files` (dọn **cả hai** prefix MinIO: theo slug và theo ObjectId)
- `backend/app/memberships/{router,admin_router}.py`: join deadline policy, `POST /leave`, `DELETE member` có điều kiện (dọn object của bài chưa `completed`), `active_total`
- `backend/app/leaderboard/service.py`: `leaderboard_response` (participant, phân trang + `me`) vs `admin_leaderboard_response` (full list); từ ADR-035 cả hai (và XLSX export dùng chung `ranked_entries`) lọc qua `eligible_query` nên không thể lệch nhau về bài bị từ chối
- `frontend/src/components/ArtifactLinks.tsx`, `frontend/src/lib/downloadArtifact.ts`: nút tải artifact dùng chung cho lịch sử participant, bảng admin theo cuộc thi và bảng toàn cục - nhận `basePath` nên cùng một component gọi được cả route participant lẫn route admin
- `frontend/src/components/AdminSubmissionsPanel.tsx`, `frontend/src/pages/AdminSubmissionsPage.tsx`: danh sách toàn cục dạng thẻ hai tầng (lọc/sort/phân trang đều gửi lên server); lọc áp ngay + debounce, thẻ thống kê và highlight Điểm chính đều nằm ở component này; từ ADR-035 thêm **hai** filter tách biệt (`Trạng thái chấm` = `status`, `Trạng thái duyệt` = `review`), trường **Xét duyệt**, trường **Thao tác** (`Không chấp nhận`/`Khôi phục`, không có action với bài `failed`/`rejected` legacy) và banner thành công tự tắt sau 4.5 s; ba trường trạng thái dùng chung component `StatusBadge` (icon 15px **kèm chữ kết luận luôn hiện**, mỗi badge tô theo tông của chính nó, tooltip chỉ chở phần chi tiết dài và bám icon nên không tràn khung ở mọi bề rộng 360–1440px); ba trục phán quyết dùng chung ba hàm `statusTone`/`aiTone`/`reviewTone`, và hàm `cardTone` lấy **mức nặng nhất** trong ba tone đó để chọn vạch nhấn 4px ở lề trái thẻ, nên thẻ và badge không thể lệch màu; tầng nhận diện dựng thêm `BlockIcon` (`blue`/`gold`) và nút **Làm mới** trên header gọi lại đúng trang đang xem
- `frontend/src/components/SubmissionReviewModal.tsx`: `SubmissionRejectModal` (note bắt buộc, trim rỗng thì khoá nút gửi, giữ modal mở khi API lỗi); khôi phục dùng lại `ConfirmModal` có sẵn (ADR-035)
- `frontend/src/pages/MySubmissionsPage.tsx`: lịch sử participant - bài bị từ chối vẫn hiện `Đã chấm điểm`, metrics và hai nút tải, thêm badge `Không chấp nhận` + `Lý do: …`, nhưng bị loại khỏi badge `Tốt nhất` và khỏi "Điểm cao nhất trong trang" (ADR-035)
- `frontend/src/api/aiReview.ts`: kiểu + hàm gọi 4 route cấu hình AI, nhãn/tone cho state và verdict, và `hasPendingAiReview` - chỗ duy nhất biết "bài này còn đang chờ AI"
- `frontend/src/components/AiReviewSettingsPanel.tsx`: tab **Kiểm tra AI** trong chi tiết cuộc thi admin (bật/tắt, `base_url`, `model`, API key ghi-một-chiều, hai cờ `auto_review`/`participant_visible`, nút **Kiểm tra kết nối**, khối "Nguồn nội dung kiểm tra")
- `frontend/src/components/AiReviewDetailModal.tsx`: nơi **duy nhất** admin thấy finding và bằng chứng, kèm lịch sử theo `generation` giảm dần và nút chạy lại (có confirm nói rõ không đổi điểm/quyết định)
- `frontend/src/hooks/usePendingPolling.ts` + `frontend/src/test/timers.ts`: polling có trần (nhịp 8 s, tối đa 20 lượt) cho bảng/lịch sử còn bài `QUEUED`/`RUNNING`, và helper giả lập đồng hồ cho test của nó
- `backend/tests/test_submission_review.py`: 10 case chéo (authorization, validation, reject/restore, ẩn danh reviewer với participant, leaderboard/export tụt hạng, filter + `stats.completed`, quota/artifact/scoring-lock, hai update đồng thời)
- `backend/app/ai_review/` (ADR-036, một package một nhiệm vụ - đọc docstring đầu mỗi tệp trước khi sửa):
  - `constants.py`: mã trạng thái/verdict/job/snapshot, mã lỗi API, và **ba** hằng version (`PROMPT_VERSION`/`NORMALIZATION_VERSION`/`CONTEXT_POLICY_VERSION`) đi thẳng vào `cache_key`
  - `crypto.py`: Fernet cho API key provider; thiếu/sai `LLM_CONFIG_ENCRYPTION_KEY` **không** làm API/worker chết, chỉ làm nhánh AI không dùng được
  - `url_policy.py`: biên SSRF - cú pháp URL, **mọi hostname/IP công khai được chấp nhận** (ADR-037; không còn allowlist host), chặn host private/http, `follow_redirects=False`, trần byte theo số byte **thực đọc được**; kiểm lại ngay trước **mỗi** request nên DNS rebinding sau lúc lưu không đổi được đích
  - `notebook.py`: parse + normalize `.ipynb` bằng `json` stdlib thành text tất định (số cell là vị trí 1-based trong mảng `cells` **gốc**); không chạy/render/import, không `nbformat`
  - `content_snapshot.py`: revision thể lệ bất biến, chốt tại thời điểm nộp; dùng chung theo `(competition_id, content_hash)` nên sửa rồi hoàn nguyên không tạo rác
  - `prompt.py` + `models.py`: system prompt nêu rõ notebook là **bằng chứng không đáng tin** còn thể lệ là quy định (hàng rào prompt injection); output model parse bằng Pydantic `extra="forbid"` nên field lạ làm cả lượt hỏng thay vì được đoán. Riêng `participant_summary` (ADR-040) là field **tuỳ chọn** và **không** dùng helper `_text()`: `min_length=1` của helper đó biến "không có gì để nói" thành lỗi và đánh hỏng cả lượt review vì một field phụ
  - `verdict.py`: kiểm chứng phía server - mỗi finding phải trỏ đúng trang có thật, trích đúng đoạn có thật, vào cell/dòng có thật; `snippet` của model bị vứt và dựng lại từ notebook; verdict chỉ **giảm** độ chắc chắn, không bao giờ nâng
  - `provider.py`: adapter OpenAI-compatible **duy nhất** (`/chat/completions`); không có nhánh Anthropic/biến thể
  - `queue.py`: enqueue + claim nguyên tử + lease có fencing token; `lease_token`/`generation`/`run_id` có mặt trong **mọi** filter ghi nên worker mất lease không ghi đè được job của người khác
  - `settings.py`: parse/validate config theo cuộc thi và `public_config` đã che secret (`api_key_configured` thay cho ciphertext)
  - `service.py`: điều phối - chụp snapshot lúc nộp, enqueue, áp verdict, dựng projection; `admin_router.py`: 4 route cấu hình/test
  - `worker.py`: entrypoint `python -m app.ai_review.worker` chạy **tiến trình riêng** (không phải background task trong API); vòng lặp ba nhịp - thu hồi job hết lease, reconcile khoảng trống, rồi claim và chạy
- `backend/tests/test_ai_review_*.py` (12 tệp, 252 case) + `ai_review_helpers.py`: unit + API test cho cả 18 kịch bản bắt buộc, chạy **không** cần provider thật lẫn MinIO
- `frontend/worker/index.ts`: proxy `/api/*` same-origin, fail closed khi thiếu/sai `API_ORIGIN`
- `deploy/vps/auto-deploy.sh` + `deploy/vps/tests/auto-deploy.test.sh`: deployer chạy bằng root trên VM; harness chạy nó trên repo tạm với `git` thật và `docker` giả, không cần Docker/mạng/credential. Từ ADR-036 deployer biết thêm `ai-review-worker`: `BUILD_SERVICES` **loại trừ** nó (cùng image `vku-challenge-api:prod` với `api`, chỉ khác `command`, nên build hai lần là vô ích), và `release_defines_service` đọc compose **cũ** bằng `git show` để biết service đã tồn tại chưa - chỉ khi rollback ngược qua bản giới thiệu nó mới có đúng một ngoại lệ `docker compose rm -sf ai-review-worker`
- `scripts/minio_init.sh` (idempotent, chạy một lần qua `minio-init`), `scripts/minio_smoke.sh` (kiểm chứng bucket private/quyền app trên Compose project cô lập), `scripts/backup_prod.sh`
- `backend/tests/fake_minio.py`: MinIO giả in-memory cho unit test - unit suite không cần container
- `backend/tests/helpers.py`: fixture dùng chung (`configure_scoring`, `publish_competition`) - `backend/tests` là package nên import qua `tests.helpers`

## 5. Runtime/services
- Kiến trúc web/api/mongo và volume `/data` giữ nguyên; **thêm một service `minio` + một job một lần `minio-init`** (cả trong `docker-compose.yml` và `docker-compose.prod.yml`). MinIO chỉ nằm trong network nội bộ compose: không publish port, không route Nginx, không console công khai
- Credential tách đôi: `MINIO_ROOT_USER/PASSWORD` chỉ service `minio` + `minio-init` nhận; `MINIO_ACCESS_KEY/SECRET_KEY` (app credential, quyền hạn chế trong đúng một bucket) chỉ API nhận
- Không có collection mới ở đợt ADR-034: `resources` nằm trong `competitions`, `me` là dữ liệu derived, và hạn mức/ngày là hai field `quota_day`/`quota_used` trên document `competition_memberships` đã có - membership cũ tự có bộ đếm ở lượt nộp đầu tiên, **không** migration. Artifact **không** nằm trong `DATA_DIR` nữa (chỉ record legacy còn ở đó). ADR-036 **thêm ba** collection mới (`competition_content_revisions`, `ai_review_jobs`, `ai_reviews`) - xem §7
- `minio`/`minio-init` nằm **ngoài** override theo SHA của auto-deploy: deployer không build/up chúng, chỉ kiểm chúng đã bootstrap chưa rồi mới deploy
- Thêm **một** service `ai-review-worker` (ADR-036, `docker-compose.yml` + `docker-compose.prod.yml`): dùng lại **đúng image** `vku-challenge-api:prod` của `api`, chỉ khác `command` (`python -m app.ai_review.worker`), nên không có image thứ hai để build hay rollback. Không publish port, không route Nginx, không volume riêng - nó nói chuyện với Mongo trong network nội bộ. Healthcheck đọc **mtime** của `/tmp/ai-review-worker.heartbeat` (mới hơn 300 s là sống). Từ ADR-036 deployer coi nó như `minio`: có mặt thì được tạo lại theo SHA, vắng mặt ở bản cũ thì bị `rm -sf` đúng một lần lúc rollback (§4). Đây là container **thứ ba** của stack production (cùng `api` và `web`)
- Worker **vô hại khi không dùng**: chưa cuộc thi nào bật AI thì `ai_review_jobs` rỗng, worker chỉ quét và không gọi mạng; thiếu `LLM_CONFIG_ENCRYPTION_KEY` thì nhánh AI không dùng được nhưng container vẫn lên và `api` không hề hấn

## 6. Current API contract summary
- Mới (ADR-028): `POST /api/competitions/{slug}/submissions` nhận multipart **hai** phần bắt buộc (`file` = CSV dự đoán, `notebook` = `.ipynb`); `GET .../submissions/{id}/prediction` và `.../notebook` (participant, chỉ bài của mình); `GET /api/admin/submissions` (bảng toàn cục: `competition_id`/`account_id`/`status`/`sort`/`order`/`limit`/`offset`) và `GET /api/admin/submissions/{id}/{prediction,notebook}`
- Đổi (ADR-028): response 201 của submit thêm `submission_no` + `artifacts`; `GET .../submissions/me` trả metadata artifact (tên file + `available`) thay vì chỉ tên file; admin list theo cuộc thi thêm `sort`/`order`
- Error code mới (ADR-028): `INVALID_NOTEBOOK_TYPE`, `NOTEBOOK_INVALID`, `NOTEBOOK_UNSUPPORTED_VERSION`, `ARTIFACT_NOT_FOUND`, `ARTIFACT_STORAGE_UNAVAILABLE` (503)
- Mới (ADR-030): `GET /api/starter-notebook` (công khai, không truy vấn DB, tên cố định `starter-notebook.ipynb`); error code `STARTER_NOTEBOOK_MISSING` (500)
- Đổi (ADR-029): `GET /api/admin/submissions` thêm `sort=competition` và trả `stats: {total, competitions, teams, completed}`; `GET /api/admin/competitions/{id}/submissions` thêm `sort` metric `f1|precision|recall` nhưng **không** nhận `competition` và **không** trả `stats`
- Đổi (ADR-031): tên file tải về dùng **một** `_` làm phân cách (`{slug}_{account}_submission-NNNN_{prediction.csv|notebook.ipynb}`)
- Đổi (ADR-032): `GET /api/competitions` trả thêm `submission_count` cho **mọi** item (list của cả khách lẫn participant, `0` khi chưa có bài); đây là aggregate derived chạy một lần cho cả trang, không phải field Mongo. `GET /api/competitions/{slug}` **không** có field này
- Mới (ADR-035): `PATCH /api/admin/submissions/{submission_id}/review` body `{"status":"rejected","note":"…"}` hoặc `{"status":"accepted"}`; thành công trả `200 {submission: <AdminSubmissionItem>}` và UI vẫn refetch list (stats/filter/page đổi theo). Lỗi: `422 VALIDATION_ERROR` (status ngoài allowlist, note trim rỗng hoặc quá 1000 ký tự, gửi kèm note khi khôi phục), `404 NOT_FOUND` (ObjectId sai/không có), `422 INVALID_TRANSITION` (bài không ở execution `completed`, ví dụ `failed`/`rejected` legacy). ADR-035 **không thêm mã lỗi nào**. Log chỉ ghi ID + quyết định + admin, **không** ghi nội dung note
- Đổi (ADR-035): cả hai GET list admin thêm query `review=accepted|rejected` (tách hẳn khỏi `status`) và trả `review` trong item; `reviewed_by`/`reviewed_at` **chỉ** có ở shape admin. `stats.completed` và `total_submissions` của leaderboard/export nay đếm **bài được tính kết quả** (`completed` và không bị từ chối), không còn là mọi bài `completed`
- Mới (ADR-036, **chưa lên production**): bốn route cấu hình AI theo cuộc thi dưới `/api/admin/competitions/{id}/ai-review` - `GET` (config đã che secret, chỉ `api_key_configured: bool`), `PUT` (field vắng mặt = giữ nguyên, `api_key` rỗng = giữ key cũ, `acknowledge_transfer` là cờ công khai chấp nhận gửi notebook tới host), `DELETE .../ai-review/api-key`, `POST .../ai-review/test` (thử cấu hình chưa lưu). Hai route theo bài nộp dưới `/api/admin/submissions/{id}/ai-review`: `GET` (projection + revision đã dùng + lịch sử theo `generation`) và `POST .../ai-review/rerun`
- Đổi (ADR-036): `POST .../submissions` (201) và `GET .../submissions/me` trả thêm `ai_review` cho participant **chỉ khi** cuộc thi bật AI và `participant_visible` - shape `{state, verdict, summary, updated_at}`, **không** có mã lỗi kỹ thuật, provider, model, finding hay bằng chứng; lượt AI không làm chậm response vì chấm điểm vẫn đồng bộ và AI chạy nền. Hai GET list admin nhận thêm query `ai_review=all|none|pending|clear|flagged|inconclusive|error` (trục **thứ ba**, AND với `status` và `review`) và trả `ai_review` trong item; `stats` vẫn chỉ tính trên `status`+`review`
- Đổi (ADR-040): `ai_review` trong hai GET list admin thêm `participant_summary` (`string | null`) và mỗi lượt trong `history[]` của `GET /api/admin/submissions/{id}/ai-review` cũng có field này. Đây là **gợi ý ngắn model soạn nháp cho thí sinh**; nó là **admin-only**. Shape participant **không đổi** (vẫn đúng bốn khoá `{state, verdict, summary, updated_at}`), và `participant_summary` **không** xuất hiện trong bất kỳ response participant nào. ADR-040 **không thêm route, không thêm mã lỗi**: field là tuỳ chọn, thiếu nó vẫn là một lượt review hợp lệ
- Error code mới (ADR-036): nhóm cấu hình `AI_CONFIG_INCOMPLETE`, `AI_ENCRYPTION_KEY_MISSING`, `AI_API_KEY_MISSING`, `AI_ENDPOINT_INVALID`, `AI_PRIVATE_HOST_NOT_ALLOWED`, `AI_INSECURE_ENDPOINT_NOT_ALLOWED`, `AI_TRANSFER_NOT_ACKNOWLEDGED`; nhóm trạng thái `AI_REVIEW_DISABLED`, `AI_REVIEW_NOT_AVAILABLE`, `AI_REVIEW_IN_PROGRESS`, `AI_NOTEBOOK_MISSING`, `AI_CONTENT_SNAPSHOT_UNAVAILABLE`, `AI_CONTENT_TOO_LARGE`; nhóm provider `AI_CONNECTION_FAILED`, `AI_PROVIDER_UNAUTHORIZED`, `AI_PROVIDER_REQUEST_REJECTED` (ADR-038: 400/404/422 đều về đây; `AI_PROVIDER_MODEL_INVALID` cũ không còn đường sinh), `AI_PROVIDER_RATE_LIMITED`, `AI_PROVIDER_UNAVAILABLE`, `AI_PROVIDER_REDIRECT_REJECTED`, `AI_PROVIDER_RESPONSE_TOO_LARGE`, `AI_RESPONSE_INVALID`, `AI_OUTPUT_TRUNCATED` (ADR-039: provider trả 200 nhưng câu trả lời vượt ngân sách output, terminal); và **bốn** mã snapshot `CONTENT_EMPTY`, `CONTENT_SNAPSHOT_TOO_LARGE`, `CONTENT_CHANGED_DURING_CAPTURE`, `CONTENT_UNREADABLE` - bốn mã này chỉ được **lưu** trong snapshot, việc chụp thể lệ lỗi **không** chặn lượt nộp, và không bao giờ trả cho participant. Danh sách đầy đủ kèm mã HTTP: `docs/API_CONTRACT.md` §5.7 + §6
- Mới (ADR-027): `POST /api/admin/competitions/{id}/reopen`
- Mới/đổi (ADR-015→ADR-019): `POST /api/competitions/{slug}/leave`; `DELETE /api/admin/competitions/{id}` (draft + closed + `confirm_slug`); `DELETE /api/admin/competitions/{id}/members/{account_id}`; `GET /api/competitions/{slug}` thêm `quota`; leaderboard participant thêm `limit`/`offset`/`has_more`/`me`; admin members thêm `active_total`; competition create/update thêm `resources`; `upload_limits` chỉ có ở admin detail. Mới (ADR-034): `422 VALIDATION_ERROR` kèm `error.details` nêu tên trường sai; các route path `{id}` của nộp bài và leaderboard nhận **cả** slug lẫn ObjectId
- `GET /api/health` **cố ý không** kiểm MinIO: nó là oracle rollback của auto-deployer, nên MinIO hỏng không được phép làm health đổi trạng thái
- Chi tiết: `docs/API_CONTRACT.md` §3-§6

## 7. Current data model and indexes
- `submissions.submission_no` (int, **absent ở record cũ**): số thứ tự thật theo `(competition_id, account_id)`, dùng làm token trong tên file khi tải **và** trong object key; từ ADR-033 số được cấp trước khi upload từ counter `submission_seq` của membership (membership cũ seed bằng số lớn nhất đã cấp), nên số có thể nhảy cách khi upload fail
- `submissions.artifacts` (`{prediction|notebook: {object_key, original_filename, size_bytes}}`, absent ở record cũ): `object_key` **không bao giờ** trả về API; record cũ vẫn đọc bằng `file_path`/`original_filename`
- `accounts.slug` (str, absent ở account cũ): segment đường dẫn artifact trên MinIO (ADR-033), sinh tự động từ `name`, bất biến, không xuất hiện trong representation nào của API; index unique **sparse** (account chưa có slug không được coi là cùng giá trị null)
- `competition_memberships.submission_seq` (int, absent ở membership cũ): counter `$inc` nguyên tử cấp `submission_no` cho cặp (cuộc thi, account)
- `submissions.review` (**object tùy chọn**, absent ở mọi record hiện có - ADR-035): `{status: "rejected"|"accepted", note: string|null, reviewed_by: ObjectId, reviewed_at: datetime UTC}`. Thiếu `review` = hợp lệ, và `$ne` của Mongo khớp cả field thiếu nên predicate `status == "completed" AND review.status != "rejected"` đúng với cả record cũ - **không migration/backfill**. Mỗi lần reject/restore ghi **toàn bộ** object bằng một `$set` trong `find_one_and_update` có điều kiện `status="completed"` (không bao giờ trộn metadata của hai admin); chỉ giữ quyết định gần nhất, không có event history. Khôi phục đặt `note=null`, không chấm lại và không đụng `status`/`metrics`/`primary_score`/`artifacts`/`submission_no`/`created_at`/counter quota
- Index mới: unique partial `(competition_id, account_id, submission_no)` (`submission_no` tồn tại) - chốt số thứ tự, record legacy không tham gia ràng buộc; `(created_at DESC, _id DESC)` và `(primary_score DESC, created_at DESC, _id DESC)` cho bảng toàn cục
- `competitions.resources` (list `{label,url}`, default `[]`; document cũ thiếu field vẫn đọc được, không migration)
- Membership: `active=false` giờ cũng do participant tự đặt qua `/leave`; admin xoá cứng chỉ khi chưa có bài `completed`
- Sắp xếp `team`/`competition` ở bảng toàn cục sắp theo **tên hiển thị** nên phải `$lookup` sang `accounts`/`competitions` trong aggregation; metric sort (`f1`/`precision`/`recall`) đi thẳng vào `metrics.<field>` và **không** thêm index (đường quản trị phụ); mọi kiểu sort kết thúc bằng tie-break `created_at`→`_id`
- `stats` của bảng toàn cục là derived: một aggregation `$facet` trên cùng query filter trả `total` + ba số đếm; không có collection/field mới. Route theo một cuộc thi vẫn dùng `count_documents`. Từ ADR-035 `stats.total` **vẫn** gồm bài bị từ chối (nó vẫn là một bản ghi trong bảng) còn `stats.completed` chỉ đếm bài được tính kết quả, nên thẻ đó đổi nhãn thành "Được tính kết quả"
- `submissions.review` **không có index riêng** (ADR-035): leaderboard/export vẫn đi qua prefix index `competition_id`/`status` hiện có rồi lọc `review` trên tập đã thu hẹp, còn filter `review` là đường quản trị phụ. Thêm index chỉ khi có số đo thật, không thêm trước
- **Ba collection mới (ADR-036)** - đây là lần đầu tiên module mới thêm collection kể từ ADR-028:
  - `competition_content_revisions`: bản thể lệ **bất biến** mà AI đối chiếu. Unique `(competition_id, content_hash)` nên sửa thể lệ rồi hoàn nguyên về nội dung cũ **không** tạo revision mới, cộng `(competition_id, created_at DESC)` để lấy bản mới nhất. Vì sao là revision chứ không đọc thể lệ "hiện tại" lúc chấm: BTC sửa thể lệ sau khi bài đã nộp thì kết luận phải vẫn dựa trên đúng văn bản thí sinh đã đọc
  - `ai_review_jobs`: một job cho mỗi bài nộp - unique `submission_id` là hàng rào chống hai job cho cùng một bài; thêm `(status, run_after)` cho vòng claim và `(status, lease_expires_at)` cho vòng thu hồi job hết lease. Chạy lại tay **không** tạo job thứ hai mà tăng `generation` trên job cũ
  - `ai_reviews`: audit **append-only**, một dòng kết thúc cho mỗi `generation`. Unique `(submission_id, generation)` chốt "một lượt chạy một dòng" kể cả khi reconciler và worker ghi cùng lúc; `(competition_id, cache_key, status)` cho đường tra cache; `(submission_id, created_at DESC)` cho lịch sử trong modal admin
- `submissions.content_snapshot` (object, absent ở record cũ): `{state: "CAPTURED"|"ERROR", revision_id, content_hash, error_code, captured_at}`. Chụp hỏng **không** chặn lượt nộp: bài vẫn được chấm, vẫn xếp hạng, vẫn qua vòng duyệt của BTC
- `submissions.ai_review` (object, absent ở record cũ): projection `{state: QUEUED|RUNNING|COMPLETED|ERROR, verdict, summary, participant_summary, generation, run_id, latest_review_id, requested_at, updated_at}` - **chỉ** là con trỏ trạng thái + bản tóm tắt an toàn, không phải nơi lưu bằng chứng. `participant_summary` (ADR-040) là gợi ý ngắn cho thí sinh, **admin-only**; `request_manual_review` xoá nó cùng lúc reset projection nên gợi ý của lượt cũ không sống dậy sau khi chạy lại
- Ba index mới trên `submissions` (ADR-036): `(ai_review.state, created_at DESC, _id DESC)` và `(ai_review.verdict, created_at DESC, _id DESC)` phục vụ reconcile nền + filter admin; **không partial** vì mongomock không kiểm chứng được biểu thức partial nên test sẽ không bảo vệ được gì. Đây là ngoại lệ có chủ đích của luật "chỉ thêm index khi có số đo" (như ADR-032/ADR-035 vẫn áp): hai index này phục vụ một vòng quét nền chạy liên tục, không phải một truy vấn quản trị thỉnh thoảng
- `submissions.artifacts.notebook.sha256` (str, absent ở record cũ): SHA-256 của **bytes notebook gốc**, dùng làm một phần `cache_key` nên sửa notebook là cache tự vô hiệu
- Predicate vào leaderboard/export **không đổi** (ADR-036): `eligible_query()` vẫn chỉ là `status == "completed"` và không bị từ chối - nó **không** đọc `ai_review`. Ba trục `status`/`review`/`ai_review` không trục nào ghi đè trục nào
- Notebook khung **không có** document Mongo: asset đóng trong image backend, xem `docs/DATA_MODEL.md` §5c
- `submission_count` của public competition list là **derived, không lưu DB**: một aggregation `$match`+`$group` trên `submissions` cho cả trang, dùng lại index có prefix `competition_id` hiện có nên không có migration/index mới (ADR-032). Khác với `submission_count` của admin, vốn đến từ `activity_counts`
- Chi tiết: `docs/DATA_MODEL.md` §3, §5c, §6, §9-§10

## 8. Environment variables in use
- Mới: `MAX_NOTEBOOK_MB` (mặc định 20); `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, `MINIO_BUCKET` (mặc định `submission-artifacts`). Production phải đổi hết giá trị MinIO khỏi mặc định dev (`openssl rand -hex 24`)
- Giữ nguyên: `MAX_UPLOAD_MB` (10), `MAX_CONTENT_MB`, `MAX_ASSET_MB`, `DATA_DIR`
- Trần body hiệu lực ở Nginx là `client_max_body_size 32m` (đủ 20 MiB notebook + 10 MiB CSV + overhead multipart)
- Worker: `API_ORIGIN` là runtime variable của Cloudflare, **không** là biến `VITE_*`
- Mới (ADR-036): `LLM_CONFIG_ENCRYPTION_KEY` (Fernet, **chỉ** ở `.env` của operator - không nằm trong Mongo, không vào log, không vào repo) và nhóm biên mạng chỉ còn là **ngoại lệ hẹp do operator khai**: `AI_REVIEW_ALLOWED_PRIVATE_HOSTS`, `AI_REVIEW_ALLOWED_HTTP_HOSTS`, `AI_REVIEW_ALLOWED_PORTS` (host công khai không cần khai gì - ADR-037). Còn lại là tham số vận hành có default hợp lý (`AI_REVIEW_POLL_INTERVAL_SECONDS` 5, `RECONCILE_INTERVAL_SECONDS` 60, `LEASE_SECONDS` 180, `HEARTBEAT_SECONDS` 45, `MAX_ATTEMPTS` 3, `CONNECT_TIMEOUT_SECONDS` 5, `REQUEST_TIMEOUT_SECONDS` 60, `MAX_RESPONSE_BYTES` 1 MiB, `MAX_SNAPSHOT_BYTES` 8 MiB, `MAX_POLICY_CHARS`/`MAX_NOTEBOOK_CHARS` 160k, `MAX_OUTPUT_TOKENS` 2500, `RECONCILE_BATCH` 100, `HEARTBEAT_FILE`)
- **Provider (base_url/model/API key) không nằm trong `.env`** mà trong Mongo, theo từng cuộc thi (ADR-036). `.env` chỉ có master key + biên mạng. Docker Compose khai nhóm AI bằng `${VAR:-}` (tuỳ chọn) chứ **không** `${VAR:?}` như Mongo/MinIO: thiếu biến AI chỉ làm tính năng AI tự tắt, không được chặn stack lên
- Bất biến cấu hình: `AI_REVIEW_HEARTBEAT_SECONDS` phải **nhỏ hơn** `AI_REVIEW_LEASE_SECONDS` (có validator); hai danh sách `_PRIVATE_HOSTS`/`_HTTP_HOSTS` là đường thoát **chỉ dành cho dev** - production không được khai

## 9. Commands verified
- `cd backend && uv run pytest -q` - **295 passed** (163s), chạy lại 2026-09-20 sau ADR-033 (+19 case: 17 của `test_account_slugs.py`, 1 case seed counter và 1 case token dùng chung key/tên tải; lượt trước đó cùng ngày sau ADR-032 là **276 passed**)
- `cd backend && uv run pytest -q tests/test_competitions_public.py tests/test_competitions_admin.py` - **51 passed** (2026-09-20, ADR-032)
- `cd frontend && npx vitest run --maxWorkers 1` - **398 passed (32 files)** sau ADR-032 (2026-09-20). Lượt chạy **trước** đó của cùng ngày là **392 passed (32 files)** (129s); giữa hai lượt có thêm 6 case sort/filter/panel của `DashboardPage.test.tsx`. Một lượt `npm test` có 2 case `AdminCompetitionDetailPage.test.tsx` đỏ do quá hạn chờ dưới tải jsdom, file đó chạy riêng **62 passed** và không thuộc phạm vi ADR-032. Cùng các lượt đó: `npx tsc -b --force` → exit 0, `npm run lint` → 0 error (22 warning có sẵn), `npm run build` → OK
- `bash deploy/vps/tests/auto-deploy.test.sh` - **154 ok, 0 fail**
- `./scripts/minio_smoke.sh` - **MinIO smoke PASS**, chạy lại 2026-09-20 sau ADR-033 (không đổi kết quả: layout key không nằm trong phạm vi script). Trên Compose project cô lập (`vku-minio-smoke`), chạy thật 2026-09-19: minio healthy, `minio-init` chạy lại không lỗi, anonymous list/GET đều `Access Denied`, app credential put/get/delete đúng bytes, MinIO không publish port ra host
- `cd frontend && npx tsc -b` - pass
- `cd frontend && npx oxlint src` - exit 0
- `cd frontend && npm run build` + `npm run cf:dry-run` - pass
- `bash -n` trên deployer/installer/harness/`backup_prod.sh`/`minio_init.sh`/`minio_smoke.sh` - pass (chạy trong `release-gate / deploy-script`)
- `docker compose -f docker-compose.prod.yml config --quiet` (base và base + override named tunnel) - pass với env giả
- Đường VPS thật: timer đã bật và đã có một lượt deploy thật qua timer (probe `POST .../reopen` trả `401 UNAUTHORIZED` trong khi đường dẫn bịa trả `404 NOT_FOUND` ⇒ route mới đã live, xem ADR-026 "Rollout")
- Browser smoke thật `node /tmp/uiverify/artifact-smoke.mjs` (Chromium headless trên stack dev có MinIO, đăng nhập thật, không mock `/api`) - **26/26 PASS** (2026-09-19): nộp hai tệp → `201` rồi tải lại đúng bytes, tên tệp `ai-challenge__Đội-01__submission-0009__{prediction.csv,notebook.ipynb}` (phân cách `__` của ADR-028, **trước** ADR-031 - lượt smoke này chạy trên code cũ), admin toàn cục `sort=primary_score`/`q=team1` đều gửi lên server và tải chéo được artifact của đội khác, không tràn ngang ở 1280 lẫn 375
- Diễn tập layout key mới trên stack dev (2026-09-20, ADR-033): `docker compose up -d --build api` rồi nộp hai lượt liên tiếp qua nginx `:8080` bằng `team1@vku.vn`, đọc bucket bằng `mc ls --recursive` với credential root - **PASS**: hai lượt nhận `submission_no` **11** và **12** dù membership chưa có counter (seed đúng từ số lớn nhất đã cấp là 10), object nằm ở `competitions/ai-challenge/accounts/doi-01/submissions/submission-0011|0012/{prediction.csv,notebook.ipynb}`, `accounts.slug` = `doi-01` sinh từ tên `Đội 01` (đúng nhánh `Đ`), `submission_seq` = 12; tải lại cả bốn artifact (hai lượt × hai loại) đều `200` đúng bytes với tên `ai-challenge_Đội-01_submission-0011_*`; object **cũ** dưới key ObjectId vẫn tải được `200` - layout trộn đúng như thiết kế, không cần migration
- Backup + restore drill trên stack dev (2026-09-19, quy trình §10 `docs/DEPLOYMENT.md`) - **PASS**: `mongodump` → `gzip -t` + `mongorestore --dryRun`; `app-data.tar.gz` đọc lại được CSV legacy theo `file_path` trong DB; `mc mirror` (đúng tag pin) → `minio-artifacts.tar.gz` 6 object; Mongo restore sang DB tạm (65 document, index unique còn nguyên) → bucket test private → mirror ngược → hai artifact đọc lại theo `object_key` **trùng sha256** với tệp gốc; bucket test + DB tạm đã dọn
- Smoke artifact trên production (2026-09-19, qua `https://vku-ai-challenge-platform.ketdoannguyen.workers.dev`) - **17/17 + 7/7 + 4/4 PASS**: nộp multipart hai part rồi tải lại đúng byte cả hai artifact, đội khác `404 NOT_FOUND`, admin tải chéo được; đường legacy (tên tải 8 ký tự cuối ObjectId, notebook 404, shape `size_bytes: null`); case âm 422 không tạo submission nào
- Backup thật trên VM production (2026-09-19) - **PASS**: `20260919T221818Z` đủ ba archive + MANIFEST sha256 khớp; sau đó bucket rỗng nên lượt `20260919T224501Z` cũng PASS với `minio-artifacts.tar.gz` 120 B. Lượt bị `SIGTERM` giữa đường trả exit 143 và **không** để lại thư mục dở dang
- Restore drill trên **đường production** (2026-09-19, từ `20260919T221818Z`) - **PASS**: `mongorestore` sang DB tạm `ai_challenge_drill` (accounts=5, competitions=2, memberships=2, submissions=1), đọc `object_key` từ DB đã restore, dựng bucket drill private, mirror ngược rồi đọc lại hai artifact **trùng sha256** với tệp đã nộp (`f86105bb…` prediction 183 B, `559eb4ec…` notebook 550 B) và `ground_truth.csv` khớp byte với `/data/app` đang chạy; bucket drill + DB tạm đã xoá
- `bash deploy/vps/tests/backup-prod.test.sh` - **27 ok, 0 fail** (docker giả; bucket có object/rỗng, mirror lỗi, ngắt giữa đường, retention, chạy chồng lấn)
- Release ADR-033 lên production (2026-09-20): merge PR #7 (`f7097fe`) rồi `vku-deploy.timer` dựng lại `api`. Bằng chứng deploy: probe `GET /api/health` + `GET /api/competitions` cách nhau 15 giây quanh thời điểm merge - `502` đúng một lượt lúc `10:19:27Z` rồi `200` trở lại và giữ ổn định suốt 10 lượt sau, tức container được thay chứ không phải health hỏng kéo dài (deployer rollback nếu revision/health không khớp). **Smoke artifact trên production chưa chạy** sau lượt release này
- `/api/health` khi container `minio` **đã dừng** (2026-09-19, production) - vẫn `200 {"status":"ok","mongo":"reachable"}`; MinIO bật lại lên `healthy`
- Lượt backup qua **đúng unit systemd** trên production (`sudo systemctl start vku-backup.service`, không gọi tay script) - **Result=success**, exit 0, `20260919T225043Z` đủ `mongo.archive.gz`/`app-data.tar.gz`/`minio-artifacts.tar.gz`/`MANIFEST.txt`; đây là đường `vku-backup.timer` chạy nên nó kiểm luôn `ExecStart` đóng băng + env của unit
- `docker compose up -d --build api web` rồi `node /tmp/uiverify/adr029-smoke.mjs` (Chromium headless, không mock `/api`, Mongo + MinIO thật, đăng nhập thật) - **46/46 PASS** (2026-09-20, lượt 2026-09-19 là 43/43): khách mở `/competitions/ai-challenge/huong-dan` không bị đẩy về `/login`, trang Hướng dẫn còn **đúng bốn khối** và đã sạch chữ `seed`/`lỗi thường gặp`/`không được thực thi`, tải được `starter-notebook.ipynb` (nbformat 4, có bước khai báo thư viện và hai bước `TODO` của thí sinh, không còn checklist/bảng lỗi), menu cuộc thi đúng thứ tự `Tổng quan → Hướng dẫn → Nộp bài → Bài đã nộp → Bảng xếp hạng`, cuộc thi không có link ngoài vẫn thấy block `Tài nguyên` với đúng notebook khung, bảy cột admin global gửi đúng `sort`/`order`/`offset=0` và đổi `aria-sort`, bốn thẻ thống kê khớp `stats` của response (`19/2/2/19`) rồi `17/1/2/17` sau khi lọc, chỉ cột `Điểm chính` mang `primary-col`, panel `Kết quả` của cuộc thi không có thống kê/cột Cuộc thi/nút `Lọc` và vẫn sort metric qua endpoint scoped, không tràn ngang ở 1280 lẫn 375
- `LD_LIBRARY_PATH=/tmp/uiverify/libs/usr/lib/x86_64-linux-gnu node /tmp/uiverify/adr032-smoke.mjs` (Chromium headless trên `vite preview`, mock `/api`, 375/768/1280) - **27/27 PASS** (2026-09-20): panel portal không bị hero cắt và không tràn viewport, `Tab` không trap (`inside=[10000000]`), `Escape` trả focus về trigger, sắp A–Z/hot/`Sắp kết thúc` ra đúng thứ tự mong đợi, lọc `Chưa tham gia` đúng `membership.active`, khách thấy nhóm tham gia bị khóa (radio khớp `:disabled`) và bấm nhãn không đổi được lựa chọn, điều khiển cuối toolbar chạm mép phải ô thống kê ở 1280 (`1223 == 1223`)
- `LD_LIBRARY_PATH=/tmp/uiverify/libs/usr/lib/x86_64-linux-gnu node /tmp/uiverify/adr032-real-stack.mjs` (composition dev thật `nginx:8080` → FastAPI → Mongo, **không** mock `/api`, `docker compose up -d --build api web` từ working tree này) - **9/9 PASS** (2026-09-20): 7 competition đều có `submission_count` dạng số và **khớp `mongosh`** (`ai-challenge=17`, `smoke-s6=2`), payload khách không lộ `member_count`/`inactive_member_count`/`created_by`, detail không có `submission_count`, danh sách đã sắp A–Z tiếng Việt, khách thấy nhóm tham gia khóa kèm lời nhắc, `Sắp kết thúc` đẩy cuộc thi đã kết thúc xuống cuối, search theo tên thật lọc đúng (`"AI Cha"` → 4/7), panel không tràn ở 375
- Đợt sửa lỗi audit UI/UX (2026-09-20, ADR-034) trên stack dev `:8080`:
  - `cd backend && .venv/bin/python -m pytest -q` - **306 passed** (131s); `cd frontend && npx vitest run` - **401 passed (32 file)**; `npx tsc -b` - exit 0
  - Quota nguyên tử (`/tmp/qafix/repro.sh`, cuộc thi QA `qa-quota-fix2`, quota 5): 3 bài tuần tự rồi **6 request song song** → `429,429,201,429,429,201` (đúng 2 `201`, 4 `429`), API `{"per_day":5,"used_today":5,"remaining":0}`, `mongosh` `quota_day=2026-09-20 quota_used=5 completed=5`. **Trước** khi sửa cùng kịch bản cho 6×`201` và 9/5. Log: `/tmp/qafix/proof-quota-fix.txt`
  - Notebook (`qa-bugfix-2026-09-20`): `cells: []` → `422 NOTEBOOK_INVALID`, notebook chỉ cell `markdown` → `422 NOTEBOOK_INVALID`, notebook có cell `code` → `201` `quota_remaining:4` ⇒ hai ca bị từ chối **không** tiêu lượt
  - Leaderboard nhận slug: `GET /api/competitions/qa-quota-fix2/leaderboard` → `200` và body **giống hệt** khi gọi bằng id (`cmp` hai file rỗng khác biệt); slug lạ → `404`
  - `422` nêu trường sai: `POST /api/auth/login` body `{}` → `details:[{identifier,"Field required"},{password,"Field required"}]`; không echo giá trị đã gửi
  - Nhãn quá hạn (cuộc thi QA `qa-expired-2026-09-20`, `status` vẫn `published`, `end_at` đã qua): dashboard thí sinh hiện badge `status-badge-lg closed` + "Đã kết thúc", bảng admin hiện "Đã kết thúc", `POST /join` vẫn `422 JOIN_DEADLINE_PASSED`
  - CSP/HSTS: `curl -I http://localhost:8080/` có `Content-Security-Policy` **enforce** (không còn `-Report-Only`) + `Strict-Transport-Security`; `/tmp/uiverify/csp-verify.mjs` quét 15 route (ẩn danh + thí sinh + admin) → 15/15 có cả hai header, **0 vi phạm CSP**, 0 lỗi JS, 0 request hỏng, không trang trắng; bundle không tham chiếu URL ngoài nào
- `node /tmp/uiverify/artifact-smoke.mjs` chạy lại trên chính code của working tree này - **26/26 PASS** (2026-09-19): nộp hai tệp `201` rồi tải lại đúng bytes, tên thật `ai-challenge_Đội-01_submission-0010_{notebook.ipynb,prediction.csv}` (một `_` mỗi phân cách, không còn `__`), admin toàn cục tải chéo được artifact đội khác `ai-challenge_Đội-01_submission-0010_prediction.csv`, lọc đội và trạng thái áp dụng ngay, ba màn không tràn ngang ở 375
- Đợt **ADR-035** trên working tree này (2026-09-20): `cd backend && uv run pytest -q` - **316 passed** (120.65s); `cd frontend && npm test -- --maxWorkers=1` - **410 passed (32 file)**; `npx tsc -b` - sạch; `npm run lint` - 0 error (chỉ warning có sẵn); `npm run build` - `tsc -b` + `vite build` OK
- End-to-end **ADR-035** trên stack dev (2026-09-20, image build từ chính working tree này, qua Nginx `localhost:8080` + Mongo + MinIO thật): tầng API `bash /tmp/e2e/adr035/run.sh` - **50/50 PASS**; tầng UI `node /tmp/uiverify/adr035-ui.mjs` (Chromium headless đăng nhập thật) - **40/40 PASS**. Cuộc thi sạch `e2e-review-170401`, hai bài f1 `1.0`/`0.5` của `Đội 01`, bài điểm cao bị từ chối: quota `2/1` và `submission_count=2` **không đổi** qua cả hai chiều, `stats.completed` `2→1→2`, leaderboard `1.0/2 → 0.5/1 → 1.0/2` không chấm lại, XLSX khớp leaderboard, artifact của bài bị từ chối vẫn tải được ở cả hai vai, `SCORING_LOCKED`/`MEMBER_HAS_SUBMISSIONS` vẫn giữ, participant chỉ thấy `{note,status}`. Chi tiết và ảnh chụp: `docs/TEST_MATRIX.md` §16
- Release **ADR-034 + ADR-035** lên production (2026-09-21): merge PR #8 (`9bd5998`) lúc `01:23Z`, probe lại lúc `01:25Z` trên hostname production (Worker → Tunnel → nginx → FastAPI) - `GET /api/health` `200 {"status":"ok","mongo":"reachable"}` và `POST /api/auth/login` với body sai kiểu trả `422 VALIDATION_ERROR` **kèm** `error.details` nêu tên trường (`identifier`), **không** echo giá trị người dùng gửi. `error.details` chỉ tồn tại từ ADR-034, và `99eb735` (ADR-034) **không** là ancestor của `release` trước PR #8, nên payload này chứng minh timer auto-deploy đã nhặt image của `9bd5998`. Đây là bằng chứng **hạ tầng**, không phải bằng chứng nghiệp vụ cho ADR-035: `review`, XLSX và artifact của bài bị từ chối **chưa** được kiểm trên production

- Đợt **ADR-036** trên working tree này (2026-09-21):
  - `cd backend && uv run pytest -q` - **540 passed** (251 s, 4372 warning; lượt ADR-035 trước đó là 316 ⇒ +224 case AI, toàn bộ case cũ vẫn xanh)
  - `cd frontend && npm test -- --maxWorkers=1` - **447 passed (34 file)** (158 s; lượt trước 410/32 ⇒ +37 case AI, gồm `AiReviewSettingsPanel.test.tsx` 12, `AiReviewDetailModal.test.tsx` 10, phần còn lại nằm ở các trang/bảng đã có)
  - `cd frontend && npx tsc -b` - sạch; `npm run lint` - 0 error (chỉ warning có sẵn); `npm run build` - `tsc -b` + `vite build` OK
  - `bash deploy/vps/tests/auto-deploy.test.sh` - **173 ok, 0 fail** (lượt ADR-035 trước đó: 157; +16 case cho `ai-review-worker`: build một lần cho hai service, `release_defines_service` đọc compose cũ, rollback `rm -sf` đúng một lần)
  - `env <biến giả> docker compose -f docker-compose.prod.yml config --quiet` - **pass**; `docker-compose.yml` (dev) có cùng service `ai-review-worker` và cùng nhóm biến AI
  - Đợt **soát lỗi review** (#10-#16) trên cùng working tree, chạy lại toàn bộ: `cd backend && uv run pytest -q` - **549 passed** (199 s; +9 case so với lượt đầu của ADR-036 là 540, không case cũ nào đỏ); `cd frontend && npm test -- --maxWorkers=1` - **447 passed (34 file)**; `npx tsc -b` - sạch; `npm run lint` - 0 error (25 warning có sẵn); `npm run build` - OK; `bash deploy/vps/tests/auto-deploy.test.sh` - **173 ok, 0 fail**; `docker compose config -q` (dev) và `docker compose -f docker-compose.prod.yml config -q` (env giả) - pass. Frontend **không đổi file nào** trong đợt này
  - Chạy lại **end-to-end trên stack dev** sau đợt sửa (build lại từ working tree, provider stub có **ghi lại nguyên văn prompt gửi đi**): delimiter giả trong notebook tới model dưới dạng `‹…›`, snippet bịa của model bị vứt và dựng lại từ notebook thật, cache hit vẫn `source=CACHE`, chạy tay tăng `generation` giữ đủ hai audit row, provider chết thì retry tới trần rồi `FAILED`/`ERROR` mà bốn bài nộp vẫn `completed` điểm `1.0` và participant chỉ thấy state + câu an toàn. Bảng quan sát chi tiết: `docs/TEST_MATRIX.md` §17 (mục "Chạy lại sau đợt soát lỗi")
  - Đợt **ADR-040** trên cùng working tree, chạy lại toàn bộ: `cd backend && uv run pytest -q` - **568 passed** (272 s; 557 → 568, đúng 11 case mới, không case cũ nào đỏ); `cd frontend && npm test -- --maxWorkers=1` - **451 passed (34 file)**; `npx tsc -b` - sạch; `npm run lint` - 0 error (chỉ warning có sẵn); `npm run build` - OK
- End-to-end **ADR-036** trên stack dev (2026-09-21, image build từ chính working tree này, qua nginx `:8080` + Mongo thật, provider là **stub OpenAI-compatible** chạy local nên không có credential thật nào bị dùng): harness `/tmp/ai-smoke/` gồm `step_a_setup.sh` … `step_i2_exhaust.sh` và bốn oracle Chromium headless (`browser*.mjs`, đăng nhập thật, **không** mock `/api`), cùng một container stub tạm `ai-smoke-stub`. Đã kiểm: nộp bài `201` kèm `ai_review.state="QUEUED"` **trong cùng response** và điểm f1 `1.0` đã có ngay (AI không làm chậm đường nộp), worker `--once` chuyển projection sang `COMPLETED`, cache hit cùng thể lệ, sửa thể lệ → revision mới + cache miss, hai cuộc thi thể lệ trái ngược cho hai kết luận trái ngược trên **cùng** một notebook, giết worker giữa chừng → lease hết hạn được thu hồi và job chạy tiếp, provider chết → `ERROR` nhưng điểm và lượt nộp nguyên vẹn, redaction participant (không có mã lỗi/provider/model/host/finding), UI admin 7 giá trị lọc + modal dựng lại đúng cửa sổ bằng chứng. Bằng chứng thô: `/tmp/ai-smoke/run/*.json` (`submit_1.json`, `leaderboard.json`, `rerun_fields.json`…) và log `log_ai-review-worker.txt`. Chi tiết theo kịch bản: `docs/TEST_MATRIX.md` §17
- End-to-end **provider thật** trên stack dev (2026-09-22, cùng stack, qua đúng route admin `.../ai-review/test` và worker thật - **không** stub, có credential thật nhưng không giá trị nào bị in ra): lượt kiểm kết nối trả **HTTP 200** `{"ok": true, "host": "opencode.ai", "model": "deepseek-v4.1-flash", "latency_ms": 1519}`. Lượt review thật **đầu tiên hỏng**, và phản hồi thô cho thấy `completion_tokens` đúng bằng trần `ai_review_max_output_tokens = 2500` với JSON bị cắt giữa chuỗi (`json_invalid: EOF while parsing a string at line 50 column 119`) - tức trần của mình, không phải model. Nâng trần lên 8000 (ADR-039) rồi chạy lại: audit row generation 4 `status: COMPLETED`, `verdict: FLAGGED`, `source: PROVIDER`, `model_verdict: FLAGGED`, `host: opencode.ai`, `model: deepseek-v4.1-flash`, `duration_ms: 17159`, `usage {prompt_tokens: 5785, completion_tokens: 3508, total_tokens: 9293}`, 4 finding, `downgrades: ["RULE_NOT_FOUND"]`; projection participant `{"state": "COMPLETED", "verdict": "FLAGGED", "generation": 4}`. Kiểm rò rỉ trên log api + worker: `grep -cE "sk-|x-opencode-session|Bearer |PARTICIPANT_NOTEBOOK"` trả **0**; row tuần tự hoá có `has_key: false`/`has_prompt: false`. Quyết định: ADR-038 (session header) + ADR-039 (trần output) trong `docs/DECISIONS.md`
- End-to-end **ADR-040** trên stack dev (2026-09-22, cùng stack, **provider thật** `opencode.ai`/`deepseek-v4.1-flash`, không stub; image build lại từ chính working tree này): chạy lại tay bài nộp `6ab1e51ae1c926971c8e5a7f` (cuộc thi `ai-challenge`) qua đúng route `POST /api/admin/submissions/{id}/ai-review/rerun` → generation 6, audit row `COMPLETED`/`FLAGGED`/`source: PROVIDER`/`model_verdict: FLAGGED` với `versions.prompt = "ai-review-v3"` (bump của ADR-040 có hiệu lực thật, không phải cache cũ), 2 finding, và `participant_summary = "Đổi OUTPUT_FILE thành submission.csv và cột dự đoán thành label."` - **đúng một câu tiếng Việt, 10 từ**, nêu cả lỗi lẫn cách sửa. Projection `submissions.ai_review` mang cùng chuỗi đó; response list admin cũng vậy. **Ranh giới participant kiểm trên dữ liệu thật**: cùng bài nộp đó qua `GET /api/competitions/ai-challenge/submissions/me` chỉ trả `ai_review` với **đúng bốn khoá** `{state, verdict, summary, updated_at}`, `summary` là câu cố định *"AI phát hiện dấu hiệu cần ban tổ chức xem lại."* - cả prose dài của model lẫn `participant_summary` đều **không** xuất hiện ở đâu trong payload. **UI (Chromium headless, đăng nhập thật, không mock `/api`)**: lọc `AI: Có dấu hiệu` → mở modal từ chối → ô lý do **điền sẵn nguyên văn** gợi ý, kèm dòng nhắc nguồn gốc và `aria-describedby="review-note-suggestion review-note-help"`, nút gửi đang bật; sửa thành *"Dùng dữ liệu ngoài cuộc thi; bỏ tệp ngoài và nộp lại."* rồi gửi → banner *"Đã đánh dấu bài nộp là không chấp nhận."*, modal đóng, `pageerror` rỗng; lịch sử phía thí sinh sau đó mang **đúng bản đã sửa** trong `review.note` và vẫn không lộ gợi ý nguyên bản. Log `api` + `ai-review-worker` sau lượt chạy: `grep -cE "sk-|Bearer |Authorization:|api_key|\"prompt\"|<nội dung gợi ý>"` trả **0** ở cả hai. Bài nộp đã được khôi phục về đúng trạng thái trước drill (`review: {status:"accepted", note:null}`)

- Đợt **áp design VKU cho thẻ bài nộp** (2026-09-22, thuần trình bày, không có ADR): `cd frontend && npx vitest run` - **486 passed (34 file)**; `npx tsc -b` - exit 0; `npm run lint` - 0 error (25 warning có sẵn); `npm run build` - OK. Đo trên stack dev thật bằng Chromium headless (`/tmp/uiverify/rd6-design.mjs`, `rd2-gaps.mjs`, `rd2-center.mjs`, `rd6-scoped.mjs`, đăng nhập thật, **không** mock `/api`): 0px tràn ngang ở 1440/1280/1160/1024/768/414/375; thẻ cao **150px** ở 1440/1280 (mỗi tầng đúng một dòng), **213px** ở 1160/1024, **279px** ở 768/992, **584px** ở 414/375; khe cột tầng nhận diện 32px ở ≥90rem và 24px trong dải 48–90rem; 5 ô `subm-block-icon` mỗi thẻ toàn cục (4 ở tab scoped), 3 badge mỗi thẻ và mỗi badge mang tông của chính trục nó; tooltip bám icon và không tràn khung ở cả 10 bề rộng 360–1440px; tab scoped vẫn tra đúng bốn nhãn và không cắt email. Lượt đo này bắt được **một lỗi hồi quy do chính đợt này gây ra**: ô icon 28px thêm vào trường Đội đẩy cột email xuống **19px** ở 768px (và `Ad… · a…` ở 375px); đã nâng `flex-basis`/`min-width` của hai trường dài từ 6rem lên 11rem kèm một bậc khe 24px cho dải 768–1279px, đo lại email giữ **108px** ở 768px. Lý do phải đo chứ không suy: flexbox xuống dòng theo `flex-basis` chứ không theo bề rộng co được, và khe cột thật của bậc rộng là **32px** (`--space-xl`) chứ không phải 24px như giả định ban đầu - chính con số đó làm `Kết quả` rơi xuống dòng thứ hai ở 1280px trước khi thêm media query

## 10. Tests currently passing
- Backend: **568** - đợt **ADR-040** thêm **11** case so với lượt 557 (2026-09-22): `test_ai_review_verdict.py` **21→25** (field gợi ý vắng mặt vẫn parse được và ra `""` - "không có gì để nói" là câu trả lời hợp lệ, chuỗi toàn khoảng trắng strip về `""`, chuỗi được strip, vượt trần 200 ký tự thì `ValidationError`), `test_ai_review_service.py` **45→49** (gợi ý vào **cả** audit row lẫn `submissions.ai_review`, thiếu gợi ý vẫn là lượt `COMPLETED` với projection `null`, lượt cache mang gợi ý sang lượt sau, `cache_key` đổi khi `PROMPT_VERSION` đổi), `test_ai_review_api.py` **23→26** (admin thấy gợi ý ở list + `history[]`, **thí sinh không** nhận được chuỗi đó trong bất kỳ response nào, chạy lại xoá gợi ý của lượt bị thay thế); case reconcile chạm audit row ghi **trước khi** field tồn tại nay khẳng định đường đó đọc bằng `.get()`. Trước đó: đợt **ADR-037→ADR-039** thêm **8** case so với lượt 549 (2026-09-22): `test_ai_review_url_policy.py` **23→25** (bỏ allowlist host - mọi hostname/IP công khai được nhận, IPv6 literal bọc ngoặc, host công khai phân giải vào địa chỉ private vẫn bị chặn, wildcard cũ giờ chỉ là hostname không khớp gì), `test_ai_review_provider.py` **20→23** (mọi request mang `User-Agent` + session id, HTTP 400/404/422 **không** còn bị báo là "model sai", `finish_reason: "length"` thành `AI_OUTPUT_TRUNCATED` riêng), `test_ai_review_service.py` **42→45** (header phiên của worker đúng bằng `job["run_id"]`, lượt bị cắt vì trần token hỏng **không** tiêu retry). Trước đó: ADR-036 thêm **233** case trong 12 tệp: `test_ai_review_api.py` (23: phân quyền, validation, 7 giá trị lọc, rerun, legacy, cascade), `test_ai_review_service.py` (42: cache key, reconcile nhiều dạng lỗ hổng, redaction, bằng chứng dựng lại, injection - trong đó một case dùng `AutoModel.from_pretrained('vinai/phobert-base')` thật trong notebook để **hai cuộc thi thể lệ trái ngược ra hai kết luận trái ngược trên cùng một notebook**), `test_ai_review_queue.py` (29: unique index, claim nguyên tử, lease/fencing, retry + backoff), `test_ai_review_verdict.py` (21: hạ mức, bằng chứng bịa bị loại), `test_ai_review_settings.py` (17), `test_ai_review_url_policy.py` (23 case ở lượt ADR-036: wildcard/subdomain/private/http/port - nay là 25, xem đầu mục), `test_ai_review_content_snapshot.py` (14: hash chuẩn tắc + bảy bước chụp), `test_ai_review_notebook.py` (14: parse/normalize tất định, không render), `test_ai_review_worker.py` (14), `test_ai_review_provider.py` (20 case ở lượt ADR-036: kiểm lại policy, redirect, trần byte - nay là 23, xem đầu mục), `test_ai_review_crypto.py` (7), `test_ai_review_scoring_isolation.py` (9: verdict không đổi gì thuộc trục chấm, verdict bất lợi vẫn được tính vào `eligible_query()`/leaderboard, **chỉ** quyết định của người mới loại được bài, worker không bao giờ ghi trục `review`, chạy lại không tích thêm dòng eligible). Toàn bộ chạy **không** cần provider thật lẫn MinIO. Các số này là số case **thu được** (`pytest --collect-only`, tính cả case tham số hoá) và khớp đúng với `pytest -q`; 9 case cuối đến từ đợt soát lỗi review (#10-#16), mỗi sửa đổi hành vi đều có case hồi quy đi kèm. Trước đó: ADR-035 thêm `test_submission_review.py` (10 case: authorization, 404 vs `422 INVALID_TRANSITION`, validation status/note, reject giữ document + artifact + quota, participant chỉ thấy `{status,note}` không lộ reviewer, restore xoá note và giữ quyết định mới nhất, leaderboard/export loại bài bị từ chối rồi tụt về bài hợp lệ kế tiếp, filter `review` + `stats.completed`, scoring lock/retention, hai update đồng thời không trộn metadata). Trước đó: ADR-034 thêm 3 case quota nguyên tử (bộ đếm nằm trên membership + khoá ngày UTC; 6 `reserve_quota_slot` song song với quota 2 chỉ cấp 1 và 2; upload lỗi trả lại lượt rồi nộp lại thành công), 2 case notebook rỗng/chỉ markdown ở cả `test_submission_artifacts.py` và `test_submissions.py` (mọi fixture notebook hợp lệ được thêm một cell `code`), 2 case leaderboard nhận slug, 2 case `422` nêu tên trường. Trước đó: ADR-033 thêm `test_account_slugs.py` (17: `slugify` bỏ dấu tiếng Việt/ký tự nguy hiểm/cận độ dài, slug sinh một lần rồi bất biến, va chạm thêm hậu tố, fallback `team`), một case seed counter `submission_seq` từ số đã cấp trước đó và một case key/tên tải dùng chung `submission_token` trong `test_submission_artifacts.py`; đồng thời đổi case layout key (bỏ ObjectId khỏi đường dẫn) và case xoá cuộc thi (dọn cả hai prefix); ADR-028 đóng góp `test_submission_artifacts.py` (30), `test_submission_downloads.py` (11), `test_admin_submissions.py` (6); đợt ADR-029→ADR-031 thêm `test_starter_notebook.py` (3) và các case sort `competition`/metric + `stats` trong `test_admin_submissions.py`, case tên tải về một `_` trong `test_submission_artifacts.py`/`test_submission_downloads.py`; sau đó `test_starter_notebook.py` tách case nội dung khung thành hai (khai báo thư viện + bước `TODO`, và việc đã bỏ checklist/bảng lỗi) nên thành **4**; `fake_minio.py` giữ unit suite không cần MinIO thật
- Frontend: **486 (34 file)** tính theo `npm test` trên cây làm việc hiện tại - đợt áp design VKU cho thẻ bài nộp (2026-09-22) **+2** case: `AdminSubmissionsPage.test.tsx` thêm `::nút Làm mới tải lại đúng trang đang xem và tự khoá trong lúc chờ` và `::lần tải đầu hiện khung xương ba thẻ, không để trống`, đồng thời gộp hai case trạng thái thành `::mỗi trục phán quyết mang tông của chính nó, còn vạch thẻ vẫn là mức nặng nhất` và đổi tên case điểm thành `::Điểm chính đứng riêng trong hộp vàng, ba metric phụ giữ trung tính`. Trước đó: đợt bỏ vạch dọc ở ba trục phán quyết, đưa thẻ về đúng một vạch nhấn 4px ở lề trái (2026-09-22) **+1** case: `AdminSubmissionsPage.test.tsx` bỏ helper `verdictTone` và case `::mỗi trục phán quyết mang đúng tông của mình, thẻ không tô màu`, thay bằng hai case `::vạch nhấn ở lề thẻ lấy mức nặng nhất trong ba trục` và `::ba trục phán quyết không mang nền, viền hay vạch riêng - cả thẻ chỉ có một tín hiệu màu`. Con số 486 gồm cả đợt lịch sử AI của modal đang làm song song và **chưa** nằm trong commit nào, nên đọc nó như số đo của cây, không phải của một release. Trước đó: đợt bỏ nền khối màu của thẻ bài nộp (2026-09-22) **không thêm case nào**: nó đổi tên `AdminSubmissionsPage.test.tsx::viền thẻ và nền ba trục phán quyết lấy theo mức nặng nhất` thành `::mỗi trục phán quyết mang đúng tông của mình, thẻ không tô màu` và thay vế "viền thẻ" bằng khẳng định cả năm thẻ đều chỉ có lớp `subm-card`; lượt đó ghi là **474**. Trước đó: đợt điểm nhấn màu cho thẻ bài nộp (2026-09-22) thêm **1** case (`viền thẻ và nền ba trục phán quyết lấy theo mức nặng nhất`) và sửa **2** case cũ sang cấu trúc mới (thứ tự tầng xử lý, slug cuộc thi không còn hiện trong thẻ); con số ghi ở lượt trước là **448**, tức đã lệch 3 case so với thực tế 451 lượt đó - đã đối chiếu lại bằng `npm test`. Trước đó: đợt thiết kế lại danh sách bài nộp admin (2026-09-22) giữ nguyên số case nhưng đổi cách tra: `AdminSubmissionsPage.test.tsx` **+1** case cấu trúc (mỗi bài nộp là một thẻ hai tầng đúng thứ tự trường), các case sort/highlight cũ chuyển từ `columnheader`/`primary-col` sang thanh sắp xếp và `subm-result-primary-score`, `AdminCompetitionDetailPage.test.tsx` sửa cùng hướng; lượt thu gọn chiều cao cùng ngày đổi tiếp hai case trạng thái sang tra bằng `role="img"` + `aria-label` + `data-tip` (qua helper `statusIcon`), vì chữ kết luận giờ chỉ nằm trong tooltip và trong `.subm-icon-label` chỉ hiện khi không có hover. Trước đó: ADR-036 thêm 37 case: `AiReviewSettingsPanel.test.tsx` (12: nạp config **không** đưa key trở lại form, ô key rỗng giữ key cũ, đổi Base URL làm acknowledgement hết hiệu lực, URL chưa hợp lệ thì chưa xác nhận được host nào, xác nhận gắn host, nguồn nội dung từ "Quy định cuộc thi", **không** có Rule Builder), `AiReviewDetailModal.test.tsx` (10: lịch sử theo `generation` giảm dần, chạy lại qua confirm rồi gửi POST và đọc lại chi tiết, lượt lỗi chỉ hiện lỗi đã che), cộng 15 case ở các tệp đã có: `AdminSubmissionsPage.test.tsx` **+6** (cột AI, 7 giá trị lọc là trục riêng, `none` = chưa từng chạy), `MySubmissionsPage.test.tsx` **+6** (badge/khối AI của participant, có bài đang chờ thì poll rồi tự dừng ở trần), `SubmissionPage.test.tsx` **+3** (trang nộp **không** poll, chỉ nhắc xem ở lịch sử); `AdminCompetitionDetailPage.test.tsx` được sửa nhưng **không** thêm case nào. Trước đó: ADR-035 thêm 6 case ở `AdminSubmissionsPage.test.tsx` (cột Xét duyệt + bài `failed` không có action, filter `review` là trục riêng, PATCH reject gửi đúng body/đóng modal/refetch đúng trang, lỗi 422 giữ modal và lý do, khôi phục qua confirm modal, Hủy/Escape không gửi PATCH và trả focus về trigger), 1 case ở `AdminCompetitionDetailPage.test.tsx` (tab Kết quả gọi endpoint global nhưng refetch list scoped) và 2 case ở `MySubmissionsPage.test.tsx` (bài bị từ chối giữ metrics + hai artifact nhưng mất badge `Tốt nhất`/không vào điểm cao nhất; trang chỉ có bài bị từ chối thì không hiện điểm cao nhất). Trước đó: ADR-034 thêm 1 case nhãn quá hạn ở `DashboardPage.test.tsx`, 1 case ở `AdminCompetitionsPage.test.tsx` và 1 case trạng thái rỗng "chưa có bài nộp nào" ở `AdminSubmissionsPage.test.tsx`. Trước đó: ADR-032 thêm 6 case vào `DashboardPage.test.tsx` (sắp A–Z mặc định, hot theo `submission_count`, `Sắp kết thúc`, lọc tham gia ghép với status pill, khách bị khóa nhóm tham gia, Escape/click ngoài trả focus) nên file này thành 24 case; đợt ADR-028 thêm `ArtifactLinks.test.tsx` (3) và `AdminSubmissionsPage.test.tsx`; đợt ADR-029→ADR-031 thêm `CompetitionGuidePage.test.tsx` (4) và `submissionRequirements.test.ts` (3), mở rộng `AdminSubmissionsPage.test.tsx` (13) và `CompetitionDetailPage.test.tsx`/`App.test.tsx` cho tab Hướng dẫn và notebook built-in; sau đó `CompetitionGuidePage.test.tsx` bỏ case lỗi thường gặp (khối đã xoá) nên còn **3**
- Harness deployer: **173 assert**, gồm 3 case MinIO (chưa bootstrap / healthy / compose không có MinIO), case `.env.example` chỉ là file mẫu trong repo, và 16 case ADR-036 cho `ai-review-worker` (build một lần cho hai service dùng chung image / có mặt thì được tạo lại theo SHA / vắng mặt ở bản cũ thì rollback `rm -sf` **đúng một lần** / `release_defines_service` đọc compose cũ bằng `git show`)
- Harness backup: **27 case** (`deploy/vps/tests/backup-prod.test.sh`), chạy trong `release-gate / deploy-script`
- Ma trận đầy đủ theo chức năng: `docs/TEST_MATRIX.md` (§13 là đợt ADR-028)

## 11. Known issues / technical debt (non-blocking)
- **Rủi ro còn lại của ADR-033**: object key ghép từ `(slug cuộc thi, slug đội, số thứ tự)` nên nếu một cuộc thi bị xoá rồi tạo lại **cùng slug** trong khi bước dọn prefix best-effort đã thất bại (`files_removed:false`), lượt nộp mới có thể trỏ vào đúng key cũ và ghi đè object cũ. Không có cơ chế chặn tự động - chỉ có dấu hiệu `files_removed:false` trong response xoá cuộc thi
- **ADR-033 đã deploy nhưng chưa có bằng chứng nghiệp vụ trên production**: mới chỉ có unit test, stack dev thật (§9) và smoke MinIO cô lập. Lượt smoke sau release **bắt buộc** phải kiểm cả tên tải (đổi `__`→`_` từ ADR-031, khác thứ đã smoke ở `0864b80`) **lẫn** key thật trong bucket bằng `mc ls` trên VM - tôi không có đường vào VM nên phần key chưa ai đối chiếu. Production có ít submission nên lượt nộp tới sẽ seed `submission_seq` từ số đã cấp trước đó - đúng đường cần kiểm
- **ADR-035 đã chạy trên production nhưng chưa có bằng chứng nghiệp vụ ở đó**: unit + component test, `tsc -b`, lint, build đều xanh, hai lượt end-to-end trên stack dev sạch (API 50/50, UI 40/40 - §9), và probe sau merge PR #8 xác nhận image của `9bd5998` đã lên (dấu vết ADR-034, §9). Cái còn thiếu là **lượt kiểm nghiệp vụ trên chính production** (từ chối/khôi phục một bài thật, artifact của bài bị từ chối, XLSX và leaderboard); việc còn lại nằm ở `docs/TEST_MATRIX.md` §16 "Chưa kiểm"
- **ADR-036 chưa có bằng chứng ở đâu ngoài stack dev**: feature **tắt mặc định** và mọi bằng chứng hiện có là unit/component test + hai lượt end-to-end trên stack dev - một lượt với **provider stub** (§9, `docs/TEST_MATRIX.md` §17) và một lượt với **provider thật** ngày 2026-09-22 (§9). Lượt thật đã trả lời được ba câu mà stub không mô phỏng được: prompt thật cho ra kết luận dùng được (`FLAGGED` + 4 finding có bằng chứng), model thật trả JSON hợp lệ khi đủ ngân sách token, và độ trễ thật là ~17 s/lượt notebook nhỏ. Nó cũng **phát hiện hai lỗi thật** mà stub che mất: gateway đòi session header (ADR-038) và trần output 2500 token cắt cụt câu trả lời (ADR-039). Rủi ro **còn lại**: chi phí thật chưa đo (không có hạn mức, §11 "Chạy lại AI không có trần"), và mọi thứ vẫn chỉ ở mức **stack dev** - production chưa có `LLM_CONFIG_ENCRYPTION_KEY` lẫn container worker
- **Admin bấm "Chạy lại AI" không có trần**: chạy lại tay **luôn** bỏ qua cache (cố ý - đó là cả điểm của nút), và chỉ bị chặn bởi `409 AI_REVIEW_IN_PROGRESS` khi lượt trước còn đang chạy. Ba bộ đếm token được lưu trong mỗi `ai_reviews` nhưng **không** có hạn mức chi phí, nên một admin bấm liên tục là gọi provider liên tục. Chấp nhận ở đợt này vì đây là hành động admin có chủ đích, không phải đường participant
- **Không có màn hình hàng đợi AI**: admin chỉ thấy trạng thái theo từng bài, không có chỗ xem "còn bao nhiêu job đang chờ / đang lỗi / lỗi vì gì". Muốn biết phải vào container hoặc Mongo (`docs/DEPLOYMENT.md` §14)
- **Hai index AI trên `submissions` là non-partial** (ADR-036): cố ý, vì mongomock không kiểm chứng được biểu thức partial nên test sẽ không bảo vệ được gì. Đây là **ngoại lệ** của luật "chỉ thêm index khi có số đo" mà ADR-032/ADR-035 vẫn theo; lý do là vòng reconcile nền chạy liên tục chứ không phải một truy vấn quản trị thỉnh thoảng. Hệ quả: index phình theo số bài nộp **kể cả khi chưa cuộc thi nào bật AI**
- **ADR-037 làm biên egress rộng hơn ADR-036**: bỏ allowlist host nghĩa là **mọi** hostname/IP công khai đều ra được, không còn danh sách operator duyệt trước. Thứ giữ nội dung notebook ở đúng chỗ giờ chỉ còn hai lớp: chặn private/metadata/CGNAT khi phân giải DNS, và tick xác nhận chuyển dữ liệu gắn **từng host** ở UI. Hệ quả cần nhớ: một host công khai gõ sai (typosquat) vẫn qua được policy, và chặn private trở thành hàng rào duy nhất trước một domain công khai phân giải vào mạng nội bộ (§11, cửa sổ DNS rebinding)
- **Kết luận AI là tham khảo, không phải phán quyết**: model có thể sai, và theo thiết kế nó **không** đụng được vào điểm, trục `review` hay leaderboard (ADR-036). Đừng dùng `ai_review` để tự động loại bài - chỉ người mới làm được điều đó
- **Ba giới hạn đã biết của "biên tin cậy" trong prompt** (ADR-036): (a) chỉ **thẻ khối** được vô hiệu hoá, nên một dòng trong notebook vẫn có thể trông giống tiêu đề cell do hệ thống sinh (`=== CELL 9 | CODE ===`) - hậu quả tệ nhất là model trỏ nhầm cell, và `snippet` dựng lại từ notebook thật nên admin luôn đọc đúng văn bản ở đó; (b) `assert_network_allowed` kiểm DNS **trước** khi httpx tự phân giải, nên còn một cửa sổ DNS rebinding (`trust_env=False` siết lại nhưng không triệt tiêu, và từ ADR-037 phép kiểm private khi phân giải là hàng rào **duy nhất** cho trường hợp này); (c) notebook vượt `max_notebook_chars` bị cắt bớt và CLEAR tự hạ xuống INCONCLUSIVE kèm `DOWNGRADE_NOTEBOOK_TRUNCATED`, nhưng **chưa đo** để chọn trần - vi phạm nằm ở phần bị cắt sẽ không được nêu
- **Quyết định xét duyệt không có lịch sử**: chỉ giữ trạng thái gần nhất (ADR-035), nên không dựng lại được "ai từ chối lúc nào với lý do gì" sau khi bài được khôi phục hoặc bị từ chối lần hai - nếu BTC cần truy vết thì phải thêm event collection, đó là chủ ý không làm ở đợt này
- **Filter `review` chưa được đo hiệu năng**: `submissions.review` cố ý **không** có index (ADR-035); leaderboard/export lọc trên tập đã thu hẹp qua prefix index cũ nên không đổi độ phức tạp, nhưng filter `review` trên bảng toàn cục là quét thêm trên tập `$match`. Hai lượt end-to-end ở §9 chỉ kiểm **tính đúng**, không đo thời gian phản hồi, và dữ liệu dev chỉ có vài bài nên **chưa** có số đo nào đủ để biện minh cho index - thêm khi có dữ liệu lớn và số đo thật
- **ADR-032 đã có bằng chứng cả browser lẫn stack thật**; phần còn thiếu chỉ là đo focus ring và lượt đăng nhập participant trên stack thật. Oracle mock `/tmp/uiverify/adr032-smoke.mjs` **27/27 PASS** (panel không bị `.page-hero { overflow: hidden }` cắt, không tràn viewport ở 375/768/1280, `Tab` không trap, `Escape` trả focus, nhóm tham gia của khách hiện nhưng khóa và bấm nhãn không đổi được lựa chọn); oracle không mock `/tmp/uiverify/adr032-real-stack.mjs` **9/9 PASS** và `submission_count` khớp đúng `mongosh` trên cùng DB. Lưu ý: oracle cũ `/tmp/uiverify/vku-toolbar.mjs` đã **hỏng sẵn** (dùng `.dash-hero`, class đã đổi tên thành `.page-hero`), nên dòng §9b về mép phải ba nút lọc không còn tái lập được - bất biến thay thế là "điều khiển cuối toolbar chạm mép phải ô thống kê", đã đo ở 1280 (`1223 == 1223`). Xem `docs/TEST_MATRIX.md` §15
- Search/sort/filter của dashboard chạy **phía client trên toàn bộ list** trả về một lượt: đúng ở quy mô hiện tại, nhưng nếu danh sách cuộc thi lớn dần thì phải chuyển sang query param + phân trang server-side (ADR-032)
- `mc` (bản pin trong compose) không có `grep`/`sed`/`wc`/`cmp`: mọi so sánh byte/sha256 khi diễn tập phải làm trên host, trong container chỉ kiểm mã thoát
- Restore drill cần credential **root** để tạo bucket drill: credential app là least-privilege (chỉ bucket `submission-artifacts`, chỉ get/put/delete object) nên `mc mb` bằng app bị `Access Denied` - đúng thiết kế, không phải lỗi
- `deploy/vps/install-auto-deploy.sh` chạy bằng root nên `git rev-parse` trong repo bị từ chối (`không đọc được git HEAD`): bản đóng băng vẫn được ghi và `cmp` khớp repo, nhưng log cài đặt không ghi lại SHA nguồn. Deployer truyền `-c safe.directory=*` cho từng lệnh git, installer thì chưa
- `TS` của `scripts/backup_prod.sh` chỉ có độ phân giải giây: hai lượt chạy trong cùng một giây sẽ dùng chung tên thư mục (lượt sau ghi đè lượt trước). `flock` đã chặn chạy song song và timer chỉ chạy mỗi phút nên thực tế không xảy ra
- Bảng toàn cục sắp theo `team`/`competition` phải `$lookup` sang `accounts`/`competitions` nên không dùng được index; metric sort (`f1`/`precision`/`recall`) cố ý không có index riêng (ADR-029). Ở quy mô hiện tại (hàng nghìn bản ghi) chưa đáng lo, review nếu lớn hơn
- Notebook khung chỉ đổi được bằng sửa code + deploy, không có đường vá nóng qua UI (ADR-030) - đây là giá của việc "mọi cuộc thi đều có sẵn" mà không cần hook lúc tạo cuộc thi
- Record legacy (CSV trên `DATA_DIR`) không có migration: chúng vẫn đọc được nhưng thiếu `submission_no` (tên tải về rơi về ObjectId ngắn) và không có notebook
- Cờ `available` của bài legacy chỉ xét document có `file_path`, **không** stat đĩa: nếu tệp bị dọn tay khỏi `DATA_DIR` thì UI vẫn hiện nút tải và API trả 404, UI báo lỗi ngay trong dòng (bảng không hỏng). Bài nộp mới thì `available` phản ánh đúng vì object nằm trong MinIO
- Restore drill đã chạy trọn trên **cả** stack dev lẫn đường production, đối chiếu sha256 theo `object_key`; retention 14 ngày chỉ chạy sau khi cả Mongo, app-data và MinIO đều thành công (cố ý fail closed). Chưa kiểm: restore **thật** vào database đang phục vụ (drill chỉ restore sang DB tạm) - cần một lần diễn tập trên môi trường không phục vụ người dùng
- Limiter login process-local theo email (ADR-013): lockout 15 phút nếu kẻ xấu biết email; cần IP companion khi có Cloudflare trusted headers
- Limit 200 cho admin members: vượt 200 dòng sẽ truncate (có hiển thị total); admin accounts đã có phân trang (`limit=50` + `offset`, backend bound `1..200`/`>=0`)
- Leaderboard vẫn tính full ranking trong bộ nhớ: phù hợp 40-80 người; phân trang chỉ giảm payload/UI, không đổi độ phức tạp query
- Cascade xoá competition không có transaction (Mongo standalone): đã xoá con-trước-cha-sau + test failure injection, nhưng dọn file/object chỉ best-effort và có thể báo partial (`files_removed:false`)
- Race nhỏ giữa check `has_completed_submission` và xoá member với một submission đồng thời; backend vẫn enforce membership khi nộp nên không mất điểm đã chấm
- UI quota vẫn có thể stale trên nhiều tab (số hiển thị là ảnh chụp lúc tải trang) - nhưng cổng chặn ở backend là bộ đếm nguyên tử trên membership (ADR-034) nên tab cũ không nộp vượt được, chỉ nhận `429`
- Bộ đếm quota không tự sửa dữ liệu lịch sử: membership nào đã vượt hạn mức **trước** khi có ADR-034 (ví dụ `qa-audit-2026-09-20` 9 bài trong ngày với quota 5) sẽ seed ra `quota_used = 9` và bị chặn nộp thêm **hết ngày hôm đó** - đúng chiều fail-closed, sang ngày mới tự về mốc thật
- Hạn mức vẫn tính theo **ngày UTC** (`resets_at` 00:00 UTC), chưa theo giờ Việt Nam (F-10 trong báo cáo audit)
- Trần upload (`MAX_UPLOAD_MB`/`MAX_NOTEBOOK_MB`/`MAX_CONTENT_MB`/`MAX_ASSET_MB`) là cấu hình **theo môi trường, không lưu theo cuộc thi** (ADR-011); admin detail trả `upload_limits` để UI render hint đúng giá trị đang áp dụng
- Quick Tunnel đổi hostname mỗi lần `cloudflared` restart: deployer chỉ **cảnh báo** (kèm URL cũ/mới và đường Dashboard) chứ không tự sửa `API_ORIGIN`; tới lúc đó API qua hostname Workers trả lỗi cho tới khi sửa tay
- Cookie phiên là host-only nên session **không** dùng chung giữa hostname Workers và hostname tunnel; tại một thời điểm chỉ dùng một public origin cho người dùng thật
- UI/UX audit trình duyệt chỉ chạy ở light mode: sản phẩm có chủ đích chỉ có light mode (không dark mode, không `prefers-color-scheme`)
- Lint warnings set-state-in-effect/Fast Refresh là pattern có sẵn; không có lint error
- Branch protection **chưa bật** cho `release` lẫn `main` (kiểm 2026-09-19: `gh api .../branches/*/protection` trả `Branch not protected`), trong khi `docs/DEPLOYMENT.md` §5.1 mô tả như đã bật. Gate hiện chỉ do workflow tự chạy; bật protection là thao tác một lần trên GitHub
- Link Drive không được kiểm tra còn truy cập được (chủ đích, ADR-015) - BTC tự đảm bảo quyền chia sẻ

## 12. Decisions made this sprint
- ADR-021→024: hai trang tĩnh công khai, redesign Help Center và Giới thiệu, bố cục lưới ô full-width và bỏ hẳn khối nguồn thông tin
- ADR-025: public entry là Cloudflare Workers Static Assets + proxy `/api/*` same-origin, giữ nguyên hợp đồng cookie host-only
- ADR-026: `release` là cổng duy nhất; Actions deploy Worker, VPS tự kéo bằng systemd timer, deployer là bản copy root-owned, rollback theo image cũ đã build
- ADR-027: `closed` đảo được (`reopen`) và xoá được; `closed` không còn là bảo đảm còn lịch sử thi
- ADR-028: artifact submission (CSV + notebook) nằm trong MinIO private, tải qua FastAPI; notebook là bắt buộc; record legacy vẫn đọc được; `/api/health` không phụ thuộc MinIO. ADR-028 **thay thế một phần** ADR-003 (artifact), phần còn lại của ADR-003 giữ nguyên
- ADR-029: bảng bài nộp admin sort đủ bảy cột với hai allowlist tách biệt (scoped từ chối `competition`), bốn thống kê tính trên cả tập kết quả khớp bộ lọc qua một `$facet`, lọc áp ngay không còn nút "Lọc", highlight cột Điểm chính
- ADR-030: notebook khung là tài nguyên built-in đóng trong image phục vụ ở `GET /api/starter-notebook` (không document Mongo, không bản sao theo cuộc thi, `competitions.resources` giữ nguyên schema); tab Hướng dẫn công khai ngay sau Tổng quan, nội dung lấy từ cấu hình thật và dùng chung `submissionRequirements.ts` với tab Nộp bài
- ADR-031: tên artifact tải về dùng **một** `_` làm phân cách thay cho `__`; không cần migration vì đây là tên sinh lúc tải, không phải `object_key`
- ADR-034: hạn mức nộp bài/ngày giữ bằng bộ đếm `quota_day`/`quota_used` trên document membership, giữ chỗ bằng **một** `find_one_and_update` có điều kiện `quota_used < quota_per_day` (trước đó là đếm-rồi-hành-động, đã tái hiện 6 request song song lọt hết khi còn 2 lượt); lượt đã giữ được trả lại nếu upload/ghi DB lỗi; đổi ngày seed lại từ số bài `completed` thật nên không cần migration. Kèm theo đợt sửa lỗi audit UI/UX 2026-09-20: nhãn trạng thái suy từ `status` + `end_at`, notebook phải có ≥ 1 cell `code`, một resolver slug/id dùng chung cho nộp bài + leaderboard, `error.details` nêu tên trường sai ở `422`, CSP chuyển sang enforce + thêm HSTS, tách hai trạng thái rỗng của bảng bài nộp admin. BUG-03 của báo cáo audit **không** sửa: cho phép nới `end_at`/`quota_per_day` sau khi có điểm là thiết kế mong muốn
- ADR-033: object key artifact ghép từ `(slug cuộc thi, slug đội, số thứ tự)` thay cho ObjectId, để bucket đọc được bằng mắt và ổn định khi record bị xoá; `submission_no` cấp trước upload từ counter trên membership, `accounts.slug` sinh lazy rồi bất biến, record cũ giữ nguyên key (không migrate)
- ADR-035: xét duyệt bài nộp là **trục riêng** (`submissions.review`) chứ không đổi `submissions.status` sang `rejected`, vì `status == "completed"` đồng thời dựng lại quota, khoá scoring, chặn xoá member/artifact và chọn bài cho leaderboard/export; một predicate dùng chung (`completed` và không bị từ chối) cho bài "tốt nhất"/leaderboard/export; chỉ lưu quyết định gần nhất (latest write wins, không event history); note là dữ liệu người dùng nên **không** vào log; không migration, không index mới, không mã lỗi mới. Từ chối các phương án: thêm giá trị enum `rejected` cho `status`, tách field vô hướng rời, và collection audit event-sourced
- ADR-036: kiểm notebook bằng AI là **trục tham khảo thứ ba**, chạy **bất đồng bộ** trên một queue bền trong Mongo và một worker **tiến trình riêng**. Bốn quyết định sản phẩm chốt: (1) chỉ nói giao thức **OpenAI-compatible** phía server (`/chat/completions`) - API kiểu Anthropic hay adapter đều **bị loại**; (2) endpoint phải qua **network policy** (host công khai được mặc định; private/http/port là ngoại lệ do operator khai - ADR-037 đã bỏ vế allowlist host của quyết định này) **và** admin phải tick công khai chấp nhận rằng nội dung notebook được gửi tới host đó; (3) AI là **tuỳ chọn** - thiếu/hỏng cấu hình **không** chặn publish/nộp/chấm; (4) ADR-035 giữ nguyên - chỉ **quyết định của người** mới loại được bài khỏi leaderboard/export/thống kê. Chọn queue Mongo thay vì Redis/Celery để không thêm hạ tầng; chọn snapshot revision bất biến thay vì đọc thể lệ "hiện tại" lúc chấm; chọn verdict **chỉ hạ mức** + kiểm chứng bằng chứng phía server thay vì tin model; chọn **không** lưu raw prompt/raw response và **không** trả mã lỗi kỹ thuật cho participant; chọn API key mã hoá Fernet bằng master key ở `.env` thay vì lưu trong `ai_review_config`; và **loại** Rule Builder, slug hard-code, execute/import/render notebook, WebSocket/SSE, cùng mọi điều kiện AI trong `eligible_query()`. Từ chối phương án nhét `ai_review` vào `submissions.status` vì `status` đang gánh quota rebuild, scoring lock, chặn xoá và chọn bài cho leaderboard
- ADR-032: tìm kiếm/sắp xếp/lọc danh sách cuộc thi giữ **phía client** (endpoint trả nguyên list, chưa phân trang); `submission_count` là aggregate derived chỉ có ở public list, đếm mọi document submission đã persist; "hot" xếp theo tổng lượt nộp nhưng **không** hiển thị con số trên thẻ; nhóm lọc tham gia **hiện nhưng khóa** với khách và được ép về `Tất cả` ngay lúc render; panel dùng `createPortal` + `fieldset`/radio native thay vì ARIA menu tự dựng
- ADR-037: **bỏ hẳn allowlist host** - admin trỏ provider tới bất kỳ hostname/IP công khai nào ngay trong tab Cài đặt, không cần sửa `.env` hay restart; lời xác nhận chuyển dữ liệu theo từng host trở thành kiểm soát duy nhất gắn với người; `allowed_hosts_configured` và banner "điền `AI_REVIEW_ALLOWED_HOSTS`" bị xoá, IPv6 literal được bọc ngoặc trong authority, port ngoài policy về `AI_ENDPOINT_INVALID`. Từ chối: sentinel `*` trong allowlist (vẫn là biến env phải restart), mở luôn host nội bộ (thành SSRF primitive), và UI quản lý allowlist (thêm màn hình + collection cho một ràng buộc vừa bị bỏ)
- ADR-038: mọi request ra provider mang `User-Agent` của ứng dụng cộng một session id mờ, **không** rẽ nhánh theo hostname (`opencode.ai` trả 400 `MissingSessionID` nếu thiếu); session của worker là `job["run_id"]` vì nó đã nằm trong job/audit và queue giữ nguyên qua retry, probe dùng UUID mới không mượn danh tính ai, và session **không** vào `cache_key`; HTTP 400/404/422 về `AI_PROVIDER_REQUEST_REJECTED` và `AI_PROVIDER_MODEL_INVALID` bị xoá vì status code một mình không chứng minh model sai; body lỗi upstream vẫn **không** được đọc. Từ chối: rẽ nhánh theo host, trả body upstream cho admin, session tĩnh dùng chung, giữ mã "model sai" cho 404
- ADR-039: trần output token là **ngân sách của mình**, không phải giới hạn của model - nâng mặc định 2500 → 8000 sau khi một lượt thật đo được 3412 token cho notebook nhỏ; `finish_reason: "length"` thành mã riêng `AI_OUTPUT_TRUNCATED` (terminal, message nêu trần hiện tại + tên biến cần sửa, và kiểm **trước** `content` vì hết token có thể ra content rỗng); biến ra tới operator ở cả hai compose lẫn hai env example nhưng **chỉ** nối vào `ai-review-worker` - tiến trình duy nhất gọi provider thật. Từ chối: chỉ nâng trần mà không phát hiện, tự retry với trần lớn hơn, coi `finish_reason: length` là lỗi tạm thời, và vá JSON bị cắt (tự sáng tác nội dung)
- ADR-040: model soạn thêm **một câu gợi ý ngắn (~10 từ)** cho thí sinh (`participant_summary`), **admin-only**, dùng để **điền sẵn** ô lý do khi admin bấm "Không chấp nhận". Ba quyết định chốt: (1) **không** thêm storage/endpoint/migration - đường tới thí sinh vẫn là `review.note`, admin sửa tự do và chữ gửi đi là bản admin duyệt, nên bất biến ADR-035 ("chỉ chữ do người duyệt mới tới tay thí sinh") giữ nguyên không cần cơ chế mới; (2) độ dài là **prompt dặn + trần kỹ thuật rộng** (200 ký tự) - field **tuỳ chọn**, không dùng helper `_text()` vì `min_length=1` của nó biến "không có gì để nói" thành lỗi và đánh hỏng cả lượt review vì một field phụ; (3) chỉ điền sẵn khi `verdict === "FLAGGED"` - INCONCLUSIVE nghĩa là server vừa bác bỏ chính cáo buộc đó nên tự động hoá nó là tự động hoá một lời buộc tội chưa xác minh. Bump `PROMPT_VERSION` `ai-review-v2` → `ai-review-v3` vì prompt đổi, đổi lại tốn **một lần cache miss toàn cục**. `.get()` ở `apply_projection` là bắt buộc (reconciler và nhánh `DuplicateKeyError` đều chạm audit row ghi trước khi field tồn tại), và `request_manual_review` phải **xoá** field - thiếu bước đó thì gợi ý của lượt cũ sống dậy và admin có thể từ chối dựa trên cáo buộc đã bị thay thế. Từ chối: dùng lại `summary` dài để điền sẵn (văn xuôi kỹ thuật 1.000 ký tự viết cho admin, gửi nhầm người đọc), **tự động gửi** không cần người duyệt (phá ADR-035 và biến nội dung notebook - dữ liệu không đáng tin - thành lời buộc tội đi thẳng tới thí sinh, đúng thứ prompt injection nhắm vào), để field **bắt buộc**, thêm storage/endpoint riêng trên `submissions` cho bản đã duyệt (nguồn sự thật thứ hai cho cùng một câu chữ), và không bump version (hỏng âm thầm: cache trả row cũ không có gợi ý, admin thấy ô lý do trống mà không có lỗi nào để lần)
- Đợt áp design VKU cho thẻ bài nộp (2026-09-22, thuần trình bày nên không có ADR): bốn lựa chọn chốt với người dùng, mỗi cái là một chỗ design **không khớp** code hiện tại — (1) **giữ** quy tắc vạch nhấn "mức nặng nhất trong ba trục" thay vì mô hình accent theo `review state` của design §17, vì thẻ đang là chỗ đọc nhanh **cả ba** trục chứ không riêng xét duyệt, và design §40 cũng đã tự dặn phải khớp enum thật; (2) áp trọn nền/màu bên trong thẻ theo design §18–26, §31–38 (dải tint ở tầng nhận diện, hộp vàng cho Điểm chính, ô icon nền màu, badge tô theo tông) nhưng **vẫn** một vạch nhấn duy nhất ở lề trái, không dựng bốn tầng màu theo bốn trạng thái; (3) **không** thêm chevron mở/thu gọn (design §27) vì nội dung dài đã nằm trong tooltip và một vùng mở rộng không có dữ liệu mới chỉ làm thẻ cao thêm; (4) header chỉ thêm nút **Làm mới**, **không** thêm `Xuất CSV`/`Xuất Excel` (design §8) vì export hiện là API **theo từng cuộc thi**, danh sách toàn cục không có endpoint tương ứng - thêm nút sẽ là thêm nghiệp vụ, đúng thứ design §2 cấm. Các mục design khác bị bỏ vì **không tồn tại trong code**: trạng thái "Đang chấm điểm" (§31) - `status` chỉ có `completed`/`rejected`/`failed`; badge "Chưa xét" (§34) - `review` là `null` chứ không phải một giá trị enum; action "Duyệt" riêng (§35) - đường chấp nhận duy nhất là `Khôi phục` (ADR-035); grid template và cột "Details toggle" của §22; vạch phân cách dọc giữa các ô tầng dưới (§28); `lucide-react`/Tailwind (§41, §52, §55) vì repo **không** có icon package lẫn Tailwind - toàn bộ icon vẫn là SVG nội tuyến và CSS viết tay

## 13. Preconditions for next sprint
- Backup định kỳ **đã bật sẵn** (`vku-backup.timer` `enabled`/`active`, chạy hằng ngày ~03:20 UTC, lượt kế tiếp 2026-09-20T03:20:05Z) nên không cần thao tác gì thêm. Điều còn phải theo dõi: lượt `03:21` ngày 2026-09-19 vẫn là bản **trước** ADR-028 (chỉ 3 archive, không có `minio-artifacts.tar.gz`), nên lượt rạng sáng 2026-09-20 là lượt timer đầu tiên chạy bản backup có MinIO - kiểm bằng `journalctl -u vku-backup.service` và `ls /srv/vku-ai-challenge/backups/<timestamp>`
- Snapshot ngoài máy (GCE snapshot hoặc copy backup ra ngoài): backup cùng boot disk chỉ cứu được lỗi thao tác
- Bật branch protection cho `release` (và `main`) trên GitHub - xem §11
- ~~Chạy end-to-end ADR-035 trên stack dev trước khi commit~~ - **đã chạy 2026-09-20**, API 50/50 + UI 40/40 (§9, `docs/TEST_MATRIX.md` §16); còn lại hai mục "Chưa kiểm" ở đó (đo hiệu năng filter `review`, banner tự tắt sau 4.5 s) đều **không** chặn release
- ~~Push `main` và merge release cho đợt **ADR-034 + ADR-035**~~ - **đã xong 2026-09-21**: `main` = `bb22444`, `release` = `9bd5998` (merge PR #8, 01:23 UTC), probe `01:25Z` xác nhận đã deploy (§9)
- ~~Probe production xác nhận timer auto-deploy đã nhặt `release`~~ - **đã xong 2026-09-21** (§9)
- Còn lại: một lượt kiểm **nghiệp vụ** trên production cho ADR-035 (từ chối/khôi phục một bài thật), và chạy lại smoke tải artifact **trên đường production** vì tên tệp đổi `__`→`_` (ADR-031) và object key đổi layout (ADR-033) khác thứ đã smoke ở `0864b80`
- Đo focus ring của trigger/radio ADR-032 và chạy một lượt browser đăng nhập **participant** trên stack thật (lượt hiện tại là phiên khách) - xem §11 + `docs/TEST_MATRIX.md` §15
- **ADR-036 cần quyết định trước khi đi tiếp** (đây là việc của con người, không phải việc code):
  - **Commit hay không**: toàn bộ đợt ADR-036 đang nằm ở working tree, **chưa** commit. Hai tệp untracked `llm-ai-review.md` và `test-ui-ux.md` phải ở **ngoài** commit đó (§1)
  - **Dọn dấu vết diễn tập trên stack dev** trước khi chụp mốc: khôi phục `.env` từ `/tmp/ai-smoke/env.backup`, xoá container stub `ai-smoke-stub`, và tuỳ chọn xoá cuộc thi/bài nộp của lượt drill trong Mongo dev
  - **Chọn provider**: `LLM_CONFIG_ENCRYPTION_KEY` (sinh bằng `openssl rand`/Fernet `generate_key`) vào `/srv/vku-ai-challenge/.env` trên VM. Từ ADR-037 **không cần khai host** cho provider công khai, và **không** dùng `AI_REVIEW_ALLOWED_PRIVATE_HOSTS`/`AI_REVIEW_ALLOWED_HTTP_HOSTS` ở production (§8)
  - ~~**Một lượt gọi LLM thật** trên stack dev (không phải stub) trước khi tin prompt~~ - **đã chạy 2026-09-22** (§9): lượt đầu hỏng vì trần output của mình (ADR-039), lượt sau `COMPLETED`/`FLAGGED` với 4 finding. Không còn mục nào "chưa từng chạy" ở đây
  - Chỉ sau đó mới bàn tới lượt release ADR-036 và việc bật `participant_visible` cho cuộc thi thật

## 14. Exact next sprint
- Sprint 09: pilot - chạy một cuộc thi thật với người dùng ngoài, xác nhận lượt backup định kỳ đầu tiên có `minio-artifacts.tar.gz`, và diễn tập restore **vào môi trường không phục vụ người dùng**
- Việc ADR-036 còn treo: **chốt commit** (đợt ADR-036→ADR-039 vẫn ở working tree), dọn dấu vết diễn tập trên stack dev, rồi mới bàn tới release và việc bật `participant_visible` (§13). Lượt gọi provider thật trên stack dev **đã xong 2026-09-22** (§9)

## 15. Handoff notes for the next AI agent
- Muốn "public/private leaderboard" thì đọc ADR-020 trước: **không** nhân đôi competition; hướng đúng là một ground truth có partition và một submission sinh hai score
- Mọi thay đổi publish/join phải đi qua `app/scoring/readiness.py` và giữ thứ tự policy trong `memberships/router.py`
- Xoá dữ liệu: luôn con-trước-cha-sau, dọn file/object sau khi DB xong, không thêm force flag cho published/member đã có bài `completed`
- Artifact: chỉ `backend/app/submission_artifacts/storage.py` biết `object_key`; mọi thứ khác (API, UI, tên file) đi qua `artifact_metadata`/`download_filename`. Không thêm presigned URL, không publish bucket, **không** cho `/api/health` phụ thuộc MinIO
- Không render/execute/import nội dung notebook ở bất kỳ đâu; validate chỉ bằng `json` stdlib - áp dụng cả cho notebook khung built-in
- Bảng admin có **hai** allowlist sort: scoped (`SCOPED_SORT_FIELDS`, không có `competition`) và global (`SORT_FIELDS`). Thêm giá trị sort mới phải chọn rõ nó thuộc allowlist nào; `stats` chỉ có ở route global
- Nội dung mô tả cách nộp bài (tên cột, bảng mẫu, danh sách lỗi) chỉ được nằm ở `frontend/src/lib/submissionRequirements.ts` - thêm bản sao thứ hai ở trang khác là mở đường cho hai mô tả lệch nhau
- Không expose `created_by`/`pos_label` ra representation public; field mới phải chọn rõ bên public hay admin. Cùng luật đó áp cho `review.reviewed_by`/`reviewed_at`: participant chỉ nhận `{status, note}`
- Mọi error code mới phải vào `docs/API_CONTRACT.md` §6
- **Đừng đổi `submissions.status` để diễn đạt quyết định của admin** (ADR-035): cùng điều kiện `status == "completed"` đang gánh quota rebuild, scoring lock, chặn xoá member/artifact và chọn bài cho leaderboard/export. Mọi chỗ hỏi "bài này có được tính kết quả không" phải gọi `eligible_query()` trong `app/submissions/service.py`, không viết lại literal `status: "completed"` - participant leaderboard, admin leaderboard và XLSX export dùng chung `ranked_entries` nên **không được** để ba đường lệch nhau. Ghi `review` luôn bằng một `$set` cả object qua `find_one_and_update` (latest write wins), không nửa vời từng field
- Hai trục của bảng admin phải giữ tách biệt: `status` = trạng thái chấm điểm, `review` = quyết định của admin; đổi nhãn filter thành "trạng thái chấm"/"trạng thái duyệt" là cố ý, đừng gộp lại một tham số
- Đổi deployer/backup trong repo **không** tự áp dụng lên VM: phải chạy lại `install-auto-deploy.sh` từ commit đã duyệt
- **AI review là trục thứ ba, chỉ để tham khảo** (ADR-036): nó **không bao giờ** được ghi `status`, `review`, `metrics`, `primary_score`, counter quota, `submission_no`, metadata artifact hay quyền vào leaderboard - và `eligible_query()` **không** được có điều kiện AI. Muốn loại một bài thì phải là con người, qua `review` (ADR-035)
- Đừng dựng lại những thứ ADR-036 **cố ý** không làm: **không** Rule Builder, **không** sinh slug cứng theo cuộc thi, **không** execute/import/render notebook (validate vẫn chỉ `json` stdlib), **không** thêm Redis/Celery/Kubernetes/WebSocket/SSE, **không** lưu raw prompt hay raw response, **không** trả mã lỗi kỹ thuật/provider/model/host/finding/bằng chứng/object key cho participant
- Trước khi sửa `backend/app/ai_review/`: đọc docstring đầu tệp - mỗi tệp ở đó được tách theo **một** nhiệm vụ và phần lớn bất biến nằm ngay trong dòng đó (nhất là `verdict.py` "chỉ hạ mức" và `queue.py` "fencing token trong mọi filter ghi"). Mọi thay đổi vào prompt/normalize/context policy **phải** tăng hằng version tương ứng ở `constants.py`, nếu không cache sẽ trả kết luận cũ cho prompt mới
- Nói chuyện với provider **chỉ** qua `provider.py` và chỉ theo giao thức OpenAI-compatible; mọi lần gọi phải đi qua `url_policy` (kiểm lại ngay trước request, `follow_redirects=False`, trần byte). Không thêm nhánh Anthropic hay adapter nào khác

---
- Thẻ bài nộp admin: đừng "sửa" ba thứ design ngoài repo muốn mà đợt 2026-09-22 **cố ý** không làm - vạch nhấn lề trái lấy **mức nặng nhất trong ba trục** chứ không theo `review state`, **không** có chevron mở/thu gọn, và **không** có nút export ở header toàn cục (export là API theo từng cuộc thi). Ba trục `status`/`review`/`ai_review` vẫn là ba giá trị độc lập; thẻ chỉ *hiển thị* chúng, không được gộp thành một trạng thái tổng hợp

Rules:
- Không ghi suy đoán.
- Không ghi secret.
- Nếu một chức năng đang partial, nói rõ partial ở đâu.
- Mỗi endpoint/schema đã thay đổi phải đồng bộ sang contract file.
