# Environment / secrets
.claude/
stitch_vku_ai_challenge_platform_1/
.env
.env.*
!.env.example

# Local data (Markdown, assets, ground truth, submissions, backups)
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
coverage/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Tài liệu vận hành nội bộ: chứa địa chỉ và username SSH của production nên không đưa lên repo public.
# File vẫn nằm trong working tree để dùng hằng ngày; đây không phải chỗ chứa secret (private key và
# credential production không bao giờ được nằm trong repo).
docs/VPS_PRODUCTION_ACCESS.md
